---
phase: quick-260728-i5h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [QUICK-260728-i5h]

must_haves:
  truths:
    - "CLAUDE.md объявляет стек репозитория как Next.js 15 в обоих местах: обзорный абзац проекта и буллет `**Tech stack**` в `### Constraints`."
    - "В CLAUDE.md не осталось ни одного упоминания устаревшего мажора фреймворка."
    - "Смысл «дизайн унаследован от services/storefront 1:1» сохранён; добавлена ссылка на FBG-359 как источник апгрейда."
    - "Коммит задачи трогает ровно один файл (CLAUDE.md) и содержит ровно две изменённые строки."
    - "Посторонняя незакоммиченная правка CLAUDE.md (строка про docs/legal-docs.md в разделе Documentation) НЕ попала в коммит и возвращена в рабочую копию."
  artifacts:
    - CLAUDE.md
  key_links:
    - "CLAUDE.md (заявленный стек) ↔ package.json (`next: 15.5.20`, `react: ^19.0.0`) — package.json является источником истины."
---

<!-- planner-discipline-allow: Next.js 14 -->

<objective>
Привести строку технологического стека в `CLAUDE.md` в соответствие с фактическим
состоянием репозитория: ACTR мигрирован на Next.js 15.5.20 + React 19 коммитом
`21cfd70` (FBG-359, 17.07.2026), а `CLAUDE.md` до сих пор объявляет предыдущий мажор.
Устаревший стек в `CLAUDE.md` напрямую вводит в заблуждение агентов — этот файл
загружается как project instructions в каждую сессию.

Purpose: агент, читающий project instructions, должен получать актуальную версию
фреймворка и не применять паттерны предыдущего мажора (App Router API, async-хелперы,
типизация route handlers различаются между 14 и 15).

Output: изменённый `CLAUDE.md` (две строки) + один изолированный коммит.

**Границы скоупа (locked):**
- Правки ТОЛЬКО в `CLAUDE.md`. `README.md:20` содержит упоминание того же мажора, но
  там это описание исторической базы копирования — вне скоупа, НЕ трогать.
- `.planning/PROJECT.md` (строки 6, 49, 64) содержит те же устаревшие упоминания и
  является upstream-источником для блока `<!-- GSD:project-start source:PROJECT.md -->`
  в `CLAUDE.md`. По constraint задачи PROJECT.md не правим; риск обратного дрейфа при
  пересинке блока зафиксирован ниже в `<verification>` как follow-up, задачей не является.

**Tracer-first:** неприменимо — изменение однослойное (один документ, ноль слоёв
архитектуры для сквозной проверки). Все задачи `type="auto"`.
**TDD:** неприменимо — documentation-only (исключение из tdd-требования).

**Особое условие исполнения:** рабочая копия `CLAUDE.md` УЖЕ содержит постороннюю
незакоммиченную правку (добавленная строка про `docs/legal-docs.md` в разделе
Documentation, ~строка 83). Она не относится к этой задаче и не должна попасть в коммит.
План решает это порядком операций: сначала изолировать постороннюю правку в именованный
stash, затем править, коммитить и возвращать stash. Хунки разнесены (~строки 8/17 против
~83), конфликт при возврате исключён.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Изолировать постороннюю правку CLAUDE.md в именованный stash</name>
  <files>CLAUDE.md</files>
  <precondition>Рабочий каталог — корень репозитория ACTR: `git rev-parse --show-toplevel` возвращает `/home/lexun/work/puz/ACTR`. Все git-команды плана выполнять с явным `git -C /home/lexun/work/puz/ACTR`, так как cwd агента сбрасывается между вызовами.</precondition>
  <action>
    Снять с рабочей копии `CLAUDE.md` постороннюю незакоммиченную правку, чтобы последующий
    коммит содержал только изменение версии стека.

    Выполнить `git -C /home/lexun/work/puz/ACTR stash push -m "actr-quick-260728-i5h isolate CLAUDE.md" -- CLAUDE.md`.
    Pathspec-форма `stash push -- <path>` затрагивает только указанный файл — грязные файлы
    в `.planning/` и `.planning/PROJECT.md` остаются в рабочей копии нетронутыми, это ожидаемо
    и правильно.

    Перед stash зафиксировать в логе задачи вывод `git -C /home/lexun/work/puz/ACTR diff -- CLAUDE.md`,
    чтобы содержимое отложенной правки было восстановимо вручную, если Task 3 не сможет
    применить stash автоматически.

    Ветвление: если `git -C /home/lexun/work/puz/ACTR status --porcelain -- CLAUDE.md` пуст
    ЕЩЁ ДО stash (посторонняя правка уже закоммичена кем-то другим), stash НЕ создавать,
    отметить задачу как SKIPPED-BY-PRECONDITION в логе и передать этот факт в Task 3 —
    Task 3 тогда обязан пропустить шаг возврата stash.
  </action>
  <verify>
    <automated>git -C /home/lexun/work/puz/ACTR status --porcelain -- CLAUDE.md; test -z "$(git -C /home/lexun/work/puz/ACTR status --porcelain -- CLAUDE.md)" &amp;&amp; git -C /home/lexun/work/puz/ACTR stash list &amp;&amp; echo ISOLATED-OK</automated>
  </verify>
  <done>`git status --porcelain -- CLAUDE.md` даёт пустой вывод (файл идентичен HEAD). В `git stash list` присутствует запись с меткой `actr-quick-260728-i5h` — либо задача помечена SKIPPED-BY-PRECONDITION, если правки изначально не было.</done>
</task>

<task type="auto">
  <name>Task 2: Обновить версию фреймворка в двух строках CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>CLAUDE.md — строки 1–25 (блок `<!-- GSD:project-start source:PROJECT.md -->`: вводный абзац и секция `### Constraints`).</read_first>
  <action>
    Ровно две точечные замены, обе внутри блока `GSD:project-start`. Ничего больше в файле
    не менять: ни форматирование, ни переносы строк, ни другие секции.

    Замена A — вводный абзац проекта (~строка 8, строка, содержащая `services/storefront`
    в скобках после жирного `**american-creator.ru**`). Заменить эту строку целиком на:

    `**american-creator.ru** (\`services/storefront\`, Next.js 15 + MUI), но работающая по **ARM Portal`

    Перенос строки в конце сохранить как в оригинале — абзац продолжается на следующей строке
    словами `API` (`/public/arm/storefront/*` …). Не склеивать строки.

    Замена B — буллет `- **Tech stack**:` в секции `### Constraints` (~строка 17). Заменить
    строку целиком на:

    `- **Tech stack**: Next.js 15 (15.5.20) + React 19 + MUI — дизайн унаследован от \`services/storefront\` 1:1; апгрейд фреймворка — FBG-359 (17.07.2026).`

    Смысловые требования к формулировке (выполнены в тексте выше, менять текст не нужно):
    заявлен актуальный мажор с точной версией, назван React 19, сохранён исходный смысл
    «дизайн наследуется 1:1», проставлена ссылка на тикет апгрейда. Язык — русский, стиль —
    как у остальных буллетов в `### Constraints`.

    Правку вносить инструментом Edit точечной заменой строк, не переписывать файл целиком:
    в репозитории включена нормализация окончаний строк, полная перезапись даст шумный diff.
  </action>
  <verify>
    <automated>cd /home/lexun/work/puz/ACTR &amp;&amp; test "$(grep -c 'Next\.js 15' CLAUDE.md)" = "2" &amp;&amp; test "$(grep -cE 'Next\.js[[:space:]]+1[0-4]([^0-9]|$)' CLAUDE.md)" = "0" &amp;&amp; test "$(grep -c 'React 19' CLAUDE.md)" = "1" &amp;&amp; test "$(grep -c 'FBG-359' CLAUDE.md)" = "1" &amp;&amp; test "$(git diff --numstat -- CLAUDE.md)" = "$(printf '2\t2\tCLAUDE.md')" &amp;&amp; echo EDIT-OK</automated>
  </verify>
  <done>В `CLAUDE.md` ровно 2 строки с `Next.js 15`, ноль строк с предыдущими мажорами фреймворка, по одной строке с `React 19` и `FBG-359`. `git diff --numstat -- CLAUDE.md` = `2	2	CLAUDE.md` — изменены ровно две строки и ни одной лишней.</done>
</task>

<task type="auto">
  <name>Task 3: Закоммитить только CLAUDE.md и вернуть отложенную правку</name>
  <files>CLAUDE.md</files>
  <action>
    Создать коммит строго с одним файлом и вернуть в рабочую копию отложенную в Task 1
    постороннюю правку.

    Шаг 1 — стейджить только целевой файл: `git -C /home/lexun/work/puz/ACTR add CLAUDE.md`.
    Категорически НЕ использовать `git add -A` / `git add .` — в рабочей копии присутствуют
    несвязанные грязные файлы (`.planning/PROJECT.md`, `.planning/phases/**`), они не должны
    попасть в коммит.

    Шаг 2 — коммит с сообщением:
    `docs(CLAUDE.md): стек Next.js 15 + React 19 (репо мигрирован в FBG-359)`

    Шаг 3 — вернуть stash: найти запись по метке `actr-quick-260728-i5h` в
    `git -C /home/lexun/work/puz/ACTR stash list`, взять её ссылку `stash@{N}` и выполнить
    `git -C /home/lexun/work/puz/ACTR stash pop 'stash@{N}'`. Пропустить этот шаг, если
    Task 1 отмечен SKIPPED-BY-PRECONDITION.

    Контингенция: если `stash pop` завершится конфликтом (не ожидается — отложенный хунк
    находится в разделе Documentation, ~строка 83, а закоммиченные правки в строках 8 и 17),
    НЕ дропать stash. Разрешить конфликт вручную, восстановив из зафиксированного в Task 1
    diff единственную добавленную строку в раздел Documentation между строкой про `docs/TZ.md`
    и строкой про `docs/open-questions.md`, затем `git stash drop 'stash@{N}'`. Обе правки
    (стек и Documentation) обязаны присутствовать в итоговой рабочей копии.

    Шаг 4 — убедиться, что рабочая копия `CLAUDE.md` после возврата содержит и новый стек
    (уже в HEAD), и восстановленную строку раздела Documentation.
  </action>
  <verify>
    <automated>cd /home/lexun/work/puz/ACTR &amp;&amp; test "$(git show --name-only --format= HEAD | grep -c .)" = "1" &amp;&amp; git show --name-only --format= HEAD | grep -qx 'CLAUDE.md' &amp;&amp; git show --numstat --format= HEAD | grep -qx "$(printf '2\t2\tCLAUDE.md')" &amp;&amp; test "$(git diff --numstat -- CLAUDE.md)" = "$(printf '1\t0\tCLAUDE.md')" &amp;&amp; test "$(grep -c 'Next\.js 15' CLAUDE.md)" = "2" &amp;&amp; test "$(grep -c 'legal-docs.md' CLAUDE.md)" = "1" &amp;&amp; echo COMMIT-SCOPE-OK</automated>
  </verify>
  <done>HEAD-коммит содержит ровно один файл `CLAUDE.md` с numstat `2	2`. После возврата stash `git diff --numstat -- CLAUDE.md` = `1	0	CLAUDE.md` (только восстановленная строка Documentation). В рабочей копии одновременно присутствуют новый стек (2 вхождения `Next.js 15`) и строка `legal-docs.md`. Запись stash с меткой `actr-quick-260728-i5h` больше не числится в `git stash list`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree → git history | Незакоммиченные посторонние изменения (`.planning/PROJECT.md`, `.planning/phases/**`, правка Documentation в CLAUDE.md) могут быть непреднамеренно зафиксированы в коммите задачи |

Новых сетевых/пользовательских границ доверия задача не создаёт: изменение
documentation-only, кода, зависимостей и рантайма не касается. Установок пакетов нет —
package-legitimacy gate неприменим.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QT-01 | Tampering | git commit scope (Task 3) | low | mitigate | Только `git add CLAUDE.md`; запрет `git add -A`/`git add .`; автоматический гейт `git show --name-only HEAD` = ровно 1 файл |
| T-QT-02 | Repudiation | посторонняя правка Documentation | low | mitigate | Изоляция в именованный stash (`actr-quick-260728-i5h`) + фиксация diff в лог Task 1 до stash → правка восстановима даже при потере stash |
| T-QT-03 | Information Disclosure | CLAUDE.md как project instructions | low | accept | Файл не содержит секретов; вносится только версия публичной OSS-зависимости, уже видимой в закоммиченном `package.json` |
</threat_model>

<verification>
Сквозные проверки после выполнения всех задач (выполнять из `/home/lexun/work/puz/ACTR`):

1. Согласованность с источником истины — заявленный в `CLAUDE.md` мажор совпадает с
   `package.json`: `grep -nE '"(next|react)":' package.json` даёт `next: 15.5.20` и
   `react: ^19.0.0`, `grep -n 'Next.js 15' CLAUDE.md` даёт две строки (8 и 17).
2. Скоуп соблюдён — `README.md` не изменён: `git show --name-only --format= HEAD` не
   содержит `README.md`.
3. Грязные несвязанные файлы уцелели вне коммита: `git status --porcelain .planning/`
   по-прежнему показывает изменения в `PROJECT.md` и `phases/**`.

**Follow-up (вне скоупа этой задачи, зафиксировать в SUMMARY):**
Блок `<!-- GSD:project-start source:PROJECT.md -->` в `CLAUDE.md` синхронизируется из
`.planning/PROJECT.md`, где устаревший мажор остаётся в строках 6, 49 и 64. При следующей
пересинке блока правки Task 2 будут откатаны. Отдельной quick-задачей обновить те же три
строки в `PROJECT.md` (файл уже грязный по другой причине — потребуется такая же изоляция
хунков).
</verification>

<success_criteria>
- `grep -c 'Next\.js 15' CLAUDE.md` = 2
- `grep -cE 'Next\.js[[:space:]]+1[0-4]([^0-9]|$)' CLAUDE.md` = 0
- HEAD-коммит: ровно 1 файл (`CLAUDE.md`), numstat `2	2`
- `git diff --numstat -- CLAUDE.md` = `1	0	CLAUDE.md` (вернувшаяся посторонняя правка)
- `README.md` и `.planning/PROJECT.md` не изменены и не закоммичены
</success_criteria>

<output>
Create `.planning/quick/260728-i5h-claude-md-next-js-14-next-js-15-15-5-20-/260728-i5h-SUMMARY.md` when done.
Включить в SUMMARY: SHA коммита, обе итоговые строки стека, подтверждение возврата stash
и follow-up по `.planning/PROJECT.md`.
</output>
