---
phase: 04-i18n-en-tr
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - src/app/[locale]/studios/page.tsx
  - src/app/[locale]/delivery/page.tsx
  - src/app/[locale]/contacts/page.tsx
  - src/app/[locale]/faq/page.tsx
  - src/app/[locale]/partners/page.tsx
  - src/app/[locale]/partners/shops/page.tsx
  - src/app/[locale]/partners/bloggers/page.tsx
  - src/app/[locale]/partners/schools/page.tsx
  - src/app/[locale]/not-found.tsx
  - src/app/[locale]/error.tsx
  - src/app/[locale]/global-error.tsx
  - messages/en.json
  - messages/tr.json
autonomous: true
requirements: [I18N-01]
user_setup:
  - "Tolgee MCP (project 34) для machine_translate EN→TR ключей статических страниц."
must_haves:
  truths:
    - "Информационные страницы (studios, delivery, contacts, faq) полностью через t(); без кириллицы (I18N-01)"
    - "Партнёрские страницы (partners + shops/bloggers/schools) полностью через t(); без кириллицы (I18N-01)"
    - "error.tsx и not-found.tsx локализованы через t(); global-error.tsx (вне NextIntl-провайдера на корневой ошибке) использует EN-фолбэк — без RU (I18N-01)"
    - "Добавленные ключи в обоих каталогах (en/tr), синхронны"
  artifacts:
    - path: "src/app/[locale]/studios/page.tsx"
      provides: "локализованная страница для nail-студий (самая текстовая) через t()"
      contains: "useTranslations"
    - path: "src/app/[locale]/delivery/page.tsx"
      provides: "локализованная страница доставки/оплаты через t()"
    - path: "src/app/[locale]/not-found.tsx"
      provides: "локализованный 404 через t()"
  key_links:
    - from: "src/app/[locale]/partners/page.tsx"
      to: "messages partners.* ключи"
      via: "useTranslations('partners')"
      pattern: "useTranslations"
---

## Phase Goal

**As a** покупатель в Турции, **I want to** пользоваться витриной полностью на турецком или английском (интерфейс, контент товара и SEO), **so that** я делаю покупки на привычном дизайне american-creator, работающем на ARM-инфраструктуре.

*Plan 04-04 slice:* «Я читаю все информационные и партнёрские страницы (студиям, доставка, контакты, FAQ, партнёрам/магазинам/блогерам/школам) и вижу страницы ошибок на /tr или /en — без русского текста.» Закрывает статическую часть I18N-01.

<objective>
Извлечь весь русский UI-текст из статических/информационных и партнёрских страниц + страниц ошибок в ключи (EN база + TR из Tolgee).

Purpose: это последний крупный пласт хардкод-русского (studios 38, delivery 28, partners ~61, contacts/faq ~26 строк). Без него I18N-01 (нет RU-хардкода) не закрывается. Зависит от 04-03 (общий messages/*.json — не конфликтовать).
Output: локализованные studios/delivery/contacts/faq + partners×4 + error/not-found/global-error.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-i18n-en-tr/04-CONTEXT.md
@.planning/phases/04-i18n-en-tr/04-RESEARCH.md
@.planning/phases/04-i18n-en-tr/04-PATTERNS.md
@.planning/phases/04-i18n-en-tr/04-VALIDATION.md
@.planning/phases/04-i18n-en-tr/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Локализация информационных страниц (studios / delivery / contacts / faq)</name>
  <files>src/app/[locale]/studios/page.tsx, src/app/[locale]/delivery/page.tsx, src/app/[locale]/contacts/page.tsx, src/app/[locale]/faq/page.tsx, messages/en.json, messages/tr.json</files>
  <read_first>
    - 04-PATTERNS.md §"Shared Patterns" (useTranslations vs getTranslations; server-страницы → getTranslations + setRequestLocale), §"Page Move Inventory"
    - 04-RESEARCH.md Pattern 6/7 (client vs RSC), Pattern 11 (namespacing)
    - src/app/[locale]/studios/page.tsx, delivery/page.tsx, contacts/page.tsx, faq/page.tsx
    - messages/en.json (namespaces studios.*/delivery.*/contacts.*/faq.*)
  </read_first>
  <action>
    Для каждой страницы определить server/client: client → `useTranslations(ns)`; async RSC → `await getTranslations({locale, namespace})` + `setRequestLocale(locale)` уже добавлен в 04-01 (проверить). Извлечь ВСЕ кириллические строки (заголовки, абзацы, списки, FAQ-вопросы/ответы, контактные подписи) в ключи соответствующего namespace.
    Многострочный контент (FAQ-ответы, описания) допустимо хранить как отдельные ключи; длинные абзацы — один ключ на абзац. Сохранить разметку/ссылки.
    Дописать EN+TR ключи (Tolgee MCP machine_translate). Каталоги в синхроне.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test $(grep -rnP "[А-Яа-яЁё]" "src/app/[locale]/studios/page.tsx" "src/app/[locale]/delivery/page.tsx" "src/app/[locale]/contacts/page.tsx" "src/app/[locale]/faq/page.tsx" | grep -vP ':\s*(//|\*|/\*)' | wc -l) -eq 0 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    grep-gate по 4 инфо-страницам == 0; строки через t()/getTranslations; новые ключи в обоих каталогах; build зелёный.
  </acceptance_criteria>
  <done>Информационные страницы локализованы (EN+TR).</done>
</task>

<task type="auto">
  <name>Task 2: Локализация партнёрских страниц (partners + shops/bloggers/schools)</name>
  <files>src/app/[locale]/partners/page.tsx, src/app/[locale]/partners/shops/page.tsx, src/app/[locale]/partners/bloggers/page.tsx, src/app/[locale]/partners/schools/page.tsx, messages/en.json, messages/tr.json</files>
  <read_first>
    - 04-PATTERNS.md §"Shared Patterns", §"Page Move Inventory" (partners/*)
    - src/app/[locale]/partners/page.tsx, partners/shops/page.tsx, partners/bloggers/page.tsx, partners/schools/page.tsx
    - messages/en.json (namespace partners.*)
  </read_first>
  <action>
    Извлечь кириллические строки всех 4 партнёрских страниц в `partners.*` ключи (можно подсекции: partners.shops.*, partners.bloggers.*, partners.schools.*). client → useTranslations, RSC → getTranslations. Сохранить формы/ссылки.
    Дописать EN+TR ключи (Tolgee MCP). Каталоги в синхроне.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test $(grep -rnP "[А-Яа-яЁё]" "src/app/[locale]/partners/page.tsx" "src/app/[locale]/partners/shops/page.tsx" "src/app/[locale]/partners/bloggers/page.tsx" "src/app/[locale]/partners/schools/page.tsx" | grep -vP ':\s*(//|\*|/\*)' | wc -l) -eq 0 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    grep-gate по 4 партнёрским страницам == 0; строки через ключи; новые ключи в обоих каталогах; build зелёный.
  </acceptance_criteria>
  <done>Партнёрские страницы локализованы (EN+TR).</done>
</task>

<task type="auto">
  <name>Task 3: Локализация страниц ошибок (not-found / error / global-error)</name>
  <files>src/app/[locale]/not-found.tsx, src/app/[locale]/error.tsx, src/app/[locale]/global-error.tsx, messages/en.json, messages/tr.json</files>
  <read_first>
    - 04-RESEARCH.md Pattern 5/6, §"Anti-Patterns" (global-error рендерит свой <html>)
    - src/app/[locale]/not-found.tsx, error.tsx, global-error.tsx
    - messages/en.json (namespace errors.*)
  </read_first>
  <action>
    not-found.tsx и error.tsx находятся внутри NextIntlClientProvider ([locale]/layout) → использовать `useTranslations('errors')`; извлечь кириллицу в `errors.*` ключи.
    global-error.tsx — последний рубеж: рендерит собственный `<html>` и при корневой ошибке может быть ВНЕ next-intl-провайдера, поэтому `useTranslations` ненадёжен. Использовать EN-фолбэк-строки напрямую (без RU) — это допустимо для аварийного экрана и соблюдает I18N-01 (нет русского хардкода). Если global-error остаётся под [locale], выставить `<html lang>` по доступному locale (или 'en').
    Дописать EN+TR ключи errors.* (Tolgee MCP) для not-found/error. Каталоги в синхроне.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test $(grep -rnP "[А-Яа-яЁё]" "src/app/[locale]/not-found.tsx" "src/app/[locale]/error.tsx" "src/app/[locale]/global-error.tsx" | grep -vP ':\s*(//|\*|/\*)' | wc -l) -eq 0 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    grep-gate по 3 страницам ошибок == 0; not-found/error через t(); global-error на EN-фолбэке; новые ключи errors.* в обоих каталогах; build зелёный.
  </acceptance_criteria>
  <done>Страницы ошибок локализованы (RU убран; global-error на EN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → static/info pages | публичный контент, недоверенного ввода нет |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | локализация статических страниц | accept | замена текста на t()-ключи; нет недоверенного ввода и логики |
| T-04-10 | Information disclosure | error/global-error тексты | mitigate | не выводить stack/детали ошибок в локализованных строках (общие сообщения) |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run build` зелёные.
- grep-gate: 0 не-комментарной кириллицы во всех статических/партнёрских/error-страницах.
- Manual (UAT/verify-work): открыть studios/delivery/contacts/faq/partners на /tr — весь контент TR; 404 и error на /tr — TR.
</verification>

<success_criteria>
- Все статические/информационные, партнёрские и error-страницы локализованы (EN+TR), без кириллицы — статическая часть I18N-01.
- После 04-04 единственный остаток RU-хардкода — seo.ts/layout-метаданные (закрывает 04-05).
- Все build/tsc зелёные.
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Notes |
|---|---|---|
| локализованные studios/delivery/contacts/faq | pages | namespaces studios.*/delivery.*/contacts.*/faq.* |
| локализованные partners/* (4) | pages | namespace partners.* |
| локализованные not-found/error + global-error(EN) | pages | namespace errors.* |
| messages/en.json, messages/tr.json | catalogs | финальные UI-namespaces (04-05 — только SEO/meta, RU-cleanup, Tolgee finalize) |

<output>
Создать `.planning/phases/04-i18n-en-tr/04-04-SUMMARY.md` по завершении. Зафиксировать: namespace-ключи статики/партнёров/ошибок; как обработан global-error (EN-фолбэк / расположение).
</output>
