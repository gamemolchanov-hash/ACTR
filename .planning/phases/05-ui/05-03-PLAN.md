---
phase: 05-ui
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - messages/en.json
  - messages/tr.json
  - src/app/[locale]/checkout/page.tsx
  - src/app/[locale]/checkout/checkout-consent.test.tsx
autonomous: false
requirements: [COMP-02]
must_haves:
  truths:
    - "Checkout step 2 shows two required checkboxes: KVKK and mesafeli satış"
    - "Each checkbox links to its legal page (/legal/kvkk, /legal/mesafeli-satis) in a new tab"
    - "The order cannot be placed unless both checkboxes are checked (button disabled AND handleSubmit gated)"
    - "When both consents are checked the existing Stripe checkout flow proceeds unchanged"
  artifacts:
    - path: "src/app/[locale]/checkout/page.tsx"
      provides: "agreedKvkk + agreedMesafeli state, submit gate, two consent checkboxes"
      contains: "agreedKvkk"
    - path: "src/app/[locale]/checkout/checkout-consent.test.tsx"
      provides: "Tests for the consent-gate logic"
    - path: "messages/en.json"
      provides: "checkout.consent.* keys"
      contains: "checkout.consent.required"
  key_links:
    - from: "src/app/[locale]/checkout/page.tsx"
      to: "/legal/kvkk and /legal/mesafeli-satis"
      via: "Link from @/i18n/navigation, target=_blank"
      pattern: "/legal/mesafeli-satis"
    - from: "handleSubmit gate"
      to: "createOrder"
      via: "if (!agreedKvkk || !agreedMesafeli) return"
      pattern: "agreedKvkk"
---

<objective>
Slice B of Phase 5 compliance UI: two REQUIRED consent checkboxes in checkout step 2 — KVKK
(personal-data processing) and «mesafeli satış sözleşmesi + ön bilgilendirme» — per D-04. Both
gate order placement using the Phase 3 register terms-gate pattern, each links its legal page in a
new tab (D-05, D-08 checkout half). The existing Stripe checkout submit flow must remain unbroken.

Purpose: COMP-02's consent half — a TR shopper must affirmatively accept both legal consents
before an order is created.
Output: agreedKvkk/agreedMesafeli state + submit gate + two checkboxes in checkout,
checkout.consent.* i18n keys (EN+TR), a consent-gate test, and a human verification of the gate.
</objective>

## Phase Goal

**As a** Turkish shopper, **I want to** accept the KVKK and distance-sales consents (with links to
the full text) before paying, **so that** my order is placed only after I have agreed to the
legally required terms.

*(Derived from Phase 5 ROADMAP goal + locked decisions D-04/D-05/D-08.)*

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui/05-CONTEXT.md
@.planning/phases/05-ui/05-RESEARCH.md
@.planning/phases/05-ui/05-PATTERNS.md
@.planning/phases/05-ui/05-VALIDATION.md

# Analog files (read before editing) — register terms-gate is the exact pattern to copy
@src/app/[locale]/login/register/page.tsx
</context>

<interface_context>
- **Register terms-gate** (`src/app/[locale]/login/register/page.tsx`, verified): `const [agreed, setAgreed] = useState(false)` (line 87); gate `if (!agreed) { setError(...); return; }` (line 154); `const isValid = … && agreed` (line 190); `disabled={loading || !isValid}` (line 440); MUI Checkbox + FormControlLabel + Link JSX lines 404–434.
- **Checkout** `src/app/[locale]/checkout/page.tsx` (verified): add state near the existing state block (~line 182); `handleSubmit` body starts ~line 345 (insert the gate at the TOP, before `setSubmitting(true)` / `createOrder`); step-2 content begins ~line 757; the "Proceed to Payment" Button is ~line 828–831 (`disabled={submitting}` becomes `disabled={submitting || !agreedKvkk || !agreedMesafeli}`). `info` style const line 71; colors `c.main` / `c['20']`.
- **Legal pages already exist** (built in 05-01): `/legal/kvkk`, `/legal/mesafeli-satis`. Link via `import { Link } from '@/i18n/navigation'` with `target="_blank" rel="noopener noreferrer"` (D-05; tab-napping mitigation).
- **messages are FLAT dotted keys** — append `checkout.consent.*` (see RESEARCH lines 364–374 for the 7 EN strings: kvkkPrefix/kvkkLink/kvkkSuffix/mesafeliPrefix/mesafeliLink/mesafeliSuffix/required).
- **MUI Checkbox/FormControlLabel** may need adding to the `@mui/material` import in checkout if not already present.
- **CRITICAL (Pitfall 3):** the gate MUST live in `handleSubmit` (not only the disabled button) so a keyboard/programmatic submit cannot bypass it.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Append checkout.consent.* i18n keys (EN base + real TR)</name>
  <files>messages/en.json, messages/tr.json</files>
  <read_first>.planning/phases/05-ui/05-RESEARCH.md (lines 364-374 EN consent strings), messages/en.json (flat-key shape)</read_first>
  <action>
    Append FLAT keys to both message files for the consent labels (D-04/D-05). EN: use the exact
    strings from RESEARCH lines 364-374 — checkout.consent.kvkkPrefix, .kvkkLink, .kvkkSuffix,
    .mesafeliPrefix, .mesafeliLink, .mesafeliSuffix, .required. TR (messages/tr.json): real Turkish —
    e.g. kvkkLink "KVKK Aydınlatma Metni", mesafeliLink "Mesafeli Satış Sözleşmesi ve Ön
    Bilgilendirme Formu", required a Turkish "please accept both consents" message, and natural
    Turkish prefix/suffix wording around the links. Maintain EN/TR key parity; do not reorder
    existing keys.
  </action>
  <verify>
    <automated>cd /home/lexun/work/puz/ACTR && node -e "const en=require('./messages/en.json'),tr=require('./messages/tr.json');const need=['kvkkPrefix','kvkkLink','kvkkSuffix','mesafeliPrefix','mesafeliLink','mesafeliSuffix','required'].map(s=>'checkout.consent.'+s);const me=need.filter(k=>!(k in en));if(me.length)throw new Error('EN missing: '+me);const mt=need.filter(k=>!(k in tr));if(mt.length)throw new Error('TR missing: '+mt);console.log('consent keys EN/TR parity OK')"</automated>
  </verify>
  <done>All 7 checkout.consent.* keys present in EN and TR; both files valid JSON; existing keys untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two required consent checkboxes + submit gate in checkout</name>
  <files>src/app/[locale]/checkout/page.tsx, src/app/[locale]/checkout/checkout-consent.test.tsx</files>
  <read_first>src/app/[locale]/login/register/page.tsx (lines 80-200 state+gate, 404-440 checkbox JSX+disabled), src/app/[locale]/checkout/page.tsx (lines 175-210 state, 340-360 handleSubmit head, 750-835 step2 + Button), .planning/phases/05-ui/05-PATTERNS.md (lines 188-256)</read_first>
  <behavior>
    - With both checkboxes unchecked, the submit gate returns early and sets the consent error (createOrder NOT called).
    - With only one checked, still blocked.
    - With both checked, the gate passes (submit proceeds to the existing Stripe flow).
    - The Proceed-to-Payment button is disabled unless both are checked.
  </behavior>
  <action>
    Implement the consent gate per D-04, copying the register terms-gate pattern. Add state near the
    checkout state block: agreedKvkk/setAgreedKvkk and agreedMesafeli/setAgreedMesafeli, both
    `useState(false)`. At the TOP of `handleSubmit` (before `setSubmitting(true)`/`createOrder`)
    insert `if (!agreedKvkk || !agreedMesafeli) { setError(t('checkout.consent.required')); return; }`
    (Pitfall 3 — gate the order creation, not just the button). Update the Proceed-to-Payment button
    to `disabled={submitting || !agreedKvkk || !agreedMesafeli}`. In step2Content, immediately before
    the Button, add two FormControlLabel+Checkbox blocks (import Checkbox, FormControlLabel from
    @mui/material if missing; Link from @/i18n/navigation): (1) KVKK — label = kvkkPrefix + a Link to
    `/legal/kvkk` (target=_blank rel="noopener noreferrer") with text kvkkLink + kvkkSuffix; (2)
    mesafeli — label = mesafeliPrefix + a Link to `/legal/mesafeli-satis` (target=_blank
    rel="noopener noreferrer") with text mesafeliLink + mesafeliSuffix. Use checkout colors (c.main /
    c['20'], `&.Mui-checked: { color: c.main }`) and `sx={{ alignItems: 'flex-start' }}` (multiline
    label). Keep consent state at the top-level CheckoutPage component so it survives step navigation
    (Pitfall 2). Do NOT alter the createOrder/Stripe logic beyond adding the early-return gate. Then
    create checkout-consent.test.tsx (vitest + RTL) covering the four behaviors above — assert the
    button-disabled state across unchecked / one-checked / both-checked, and that the consent links
    are present. If a full checkout render is too heavy for the harness, write a focused test on the
    disabled predicate via RTL queries; prefer real behavior over snapshot.
  </action>
  <verify>
    <automated>cd /home/lexun/work/puz/ACTR && npx tsc --noEmit && npx vitest run checkout-consent --passWithNoTests && grep -q "agreedKvkk" "src/app/[locale]/checkout/page.tsx" && grep -q "agreedMesafeli" "src/app/[locale]/checkout/page.tsx" && grep -q "/legal/kvkk" "src/app/[locale]/checkout/page.tsx" && grep -q "/legal/mesafeli-satis" "src/app/[locale]/checkout/page.tsx" && grep -q "noopener noreferrer" "src/app/[locale]/checkout/page.tsx"</automated>
  </verify>
  <done>`tsc --noEmit` clean; consent test green; both states + the handleSubmit gate + the disabled-button condition present; both legal links use target=_blank with rel=noopener noreferrer; createOrder/Stripe logic otherwise unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Two required consent checkboxes (KVKK + mesafeli satış) in checkout step 2, gating order placement, each linking its legal page in a new tab.</what-built>
  <how-to-verify>
    1. Start the env: ARM demo BFF `make up` (:4000) and `npm run dev` (:3003).
    2. Add an item to the cart and go to checkout; advance to step 2 (payment).
    3. Confirm both checkboxes render and the "Proceed to Payment" button is DISABLED with neither/one checked.
    4. Try to submit with both unchecked (e.g. keyboard Enter) — order must NOT be created; consent error shows.
    5. Click each consent link — they open `/[locale]/legal/kvkk` and `/[locale]/legal/mesafeli-satis` in a NEW tab.
    6. Check both boxes — the button enables and the existing Stripe Embedded Checkout step appears/proceeds exactly as before (flow unbroken).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what failed (gate bypassable, link 404, Stripe step broken, etc.).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→ARM order | consent is enforced client-side before `createOrder`; the BFF does not enforce consent (MVP) |
| consent link → external tab | `target="_blank"` opens a new browsing context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Repudiation | consent enforcement | accept | Consent gates the client `createOrder`; a direct `POST /arm/orders` bypasses it. Acceptable for MVP (RESEARCH security note); pre-go-live, pass `kvkk_accepted` in the order payload for an audit trail. |
| T-05-08 | Elevation/Tampering | consent bypass via keyboard/programmatic submit | mitigate | Gate in `handleSubmit` (not only `disabled`) — `if (!agreedKvkk || !agreedMesafeli) return` at top (Pitfall 3) |
| T-05-09 | Tampering | reverse-tabnabbing on consent links | mitigate | `rel="noopener noreferrer"` on both `target="_blank"` legal links |
| T-05-10 | Tampering | npm/pip/cargo installs | accept | No package installs (zero new deps, verified) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npx vitest run checkout-consent` green.
- `npm run build` green; `messages/{en,tr}.json` parse with `checkout.consent.*` parity.
- `grep` confirms the gate predicate, both legal links, and `rel="noopener noreferrer"` in checkout.
- Human checkpoint confirms the gate blocks/allows order placement and the Stripe flow is unbroken.
</verification>

<success_criteria>
- Two required consent checkboxes (KVKK + mesafeli satış) in checkout step 2 (D-04).
- Each links its legal page in a new tab (D-05, D-08 checkout half).
- Order placement blocked unless both checked — button disabled AND handleSubmit gated (Pitfall 3).
- Existing Stripe checkout flow unbroken.
- COMP-02 (consent half) satisfied.
</success_criteria>

<output>
Create `.planning/phases/05-ui/05-03-SUMMARY.md` when done.
</output>
