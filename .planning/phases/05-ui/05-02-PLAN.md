---
phase: 05-ui
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/lib/kdv.ts
  - src/lib/kdv.test.ts
  - messages/en.json
  - messages/tr.json
  - src/components/ProductCard.tsx
  - src/components/ProductDetail.tsx
  - src/app/[locale]/checkout/page.tsx
autonomous: true
requirements: [COMP-01]
must_haves:
  truths:
    - "Product prices on the catalog card and product detail show a «KDV Dahil» label"
    - "Checkout Order Summary shows an informational «KDV (%20)» line equal to round(subtotal - subtotal/1.20)"
    - "The KDV line is NOT added to the order total — total is unchanged"
    - "kdvFromBrutto(1000) === 167 and kdvFromBrutto(0) === 0"
  artifacts:
    - path: "src/lib/kdv.ts"
      provides: "kdvFromBrutto pure helper (fixed 20% default)"
      exports: ["kdvFromBrutto"]
    - path: "src/lib/kdv.test.ts"
      provides: "Unit tests for kdvFromBrutto (rounding + custom rate)"
    - path: "messages/en.json"
      provides: "price.kdvDahil + price.kdvLine keys"
      contains: "price.kdvDahil"
  key_links:
    - from: "src/app/[locale]/checkout/page.tsx"
      to: "kdvFromBrutto"
      via: "import from @/lib/kdv"
      pattern: "kdvFromBrutto"
    - from: "src/components/ProductCard.tsx"
      to: "price.kdvDahil"
      via: "useTranslations"
      pattern: "price.kdvDahil"
---

<objective>
Slice A of Phase 5 compliance UI: display TR KDV (VAT) per D-01/D-02/D-03. Add a pure
`kdvFromBrutto` helper (fixed 20%, D-03), the «KDV Dahil» label on product prices (D-01), and an
informational «KDV (%20)» breakdown line in the checkout Order Summary (D-02) — computed from the
KDV-inclusive subtotal and NEVER added to the total.

Purpose: ARM returns no tax field (verified), so the storefront computes the KDV portion
client-side and labels prices as KDV-inclusive — the core of COMP-01.
Output: `src/lib/kdv.ts` + test, `price.*` i18n keys (EN+TR), labels on ProductCard/ProductDetail,
KDV line in checkout summary.
</objective>

## Phase Goal

**As a** Turkish shopper, **I want to** see that displayed prices already include KDV and view the
KDV portion at checkout, **so that** I understand the tax-inclusive price and trust the total.

*(Derived from Phase 5 ROADMAP goal + locked decisions D-01/D-02/D-03.)*

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui/05-CONTEXT.md
@.planning/phases/05-ui/05-RESEARCH.md
@.planning/phases/05-ui/05-PATTERNS.md
@.planning/phases/05-ui/05-VALIDATION.md

# Analog files (read before editing)
@src/lib/money.ts
@src/lib/money.test.ts
</context>

<interface_context>
- **Test framework is vitest** (NOT Jest — RESEARCH said Jest, it is wrong; `package.json` "test": "vitest run", config `vitest.config.ts`). Run a single file: `npx vitest run kdv`. Analog test: `src/lib/money.test.ts`.
- **messages/*.json are FLAT dotted keys** (verified), append `"price.kdvDahil"`, `"price.kdvLine"`. 336 keys/file, EN+TR parity required.
- **fmtMoney signature** (verified `src/lib/money.ts`): `fmtMoney(amount: number, currency?: string, locale?: string)`. Use it to format the KDV amount.
- **ProductCard price block** at `src/components/ProductCard.tsx` lines 133–145 (`fmtMoney(product.price, 'TRY', bcp47)`); add `const t = useTranslations()` if not present.
- **ProductDetail price** at `src/components/ProductDetail.tsx` line 735 (`<Typography variant="h1">{fmtMoney(product.price,'TRY',bcp47)}</Typography>`).
- **Checkout Order Summary** at `src/app/[locale]/checkout/page.tsx`: `info` style const at line 71 (`{ fontFamily: font, fontWeight: 300, fontSize: 14, lineHeight: '14px' }`), muted color alias `c['40']`, summary JSX lines ~931–961 (Subtotal row then Discount/Shipping/Total). The KDV row goes right AFTER the Subtotal row.
- **`price.kdvLine` text is "KDV (%20):"** — the literal "20" lives in the i18n string, the rate constant lives in `kdv.ts`.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kdvFromBrutto helper + tests + price.* keys</name>
  <files>src/lib/kdv.ts, src/lib/kdv.test.ts, messages/en.json, messages/tr.json</files>
  <read_first>src/lib/money.ts (export/JSDoc style), src/lib/money.test.ts (vitest describe/it analog), .planning/phases/05-ui/05-PATTERNS.md (lines 27–78 kdv.ts + test pattern)</read_first>
  <behavior>
    - `kdvFromBrutto(1000)` === 167  (1000 - 1000/1.20 = 166.67 → round 167)
    - `kdvFromBrutto(0)` === 0
    - `kdvFromBrutto(1000, 0.10)` === 91  (custom rate)
    - default rate is 0.20 (fixed 20%, D-03)
  </behavior>
  <action>
    Create `src/lib/kdv.ts` exporting a named pure function
    `export function kdvFromBrutto(gross: number, rate = 0.20): number { return Math.round(gross - gross / (1 + rate)); }`
    with a JSDoc matching the `money.ts` style (note D-01/D-03: TR prices are KDV-inclusive; 20%
    fixed). Create `src/lib/kdv.test.ts` (vitest) covering the four behaviors above. Then append FLAT
    keys to both message files (D-01/D-02): EN `"price.kdvDahil": "KDV Dahil"`, `"price.kdvLine":
    "KDV (%20):"`; TR `"price.kdvDahil": "KDV Dahil"`, `"price.kdvLine": "KDV (%20):"` (these TR
    terms are the same Turkish strings). Maintain EN/TR key parity; do not reorder existing keys.
  </action>
  <verify>
    <automated>cd /home/lexun/work/puz/ACTR && npx tsc --noEmit && npx vitest run kdv && node -e "const en=require('./messages/en.json'),tr=require('./messages/tr.json');if(!en['price.kdvDahil']||!en['price.kdvLine'])throw new Error('missing price keys');if(!('price.kdvDahil' in tr)||!('price.kdvLine' in tr))throw new Error('TR parity gap');console.log('price keys OK')"</automated>
  </verify>
  <done>`kdv.ts` exports `kdvFromBrutto`; `npx vitest run kdv` green for all four cases; `price.kdvDahil`/`price.kdvLine` present in EN and TR; `tsc --noEmit` clean.</done>
</task>

<task type="auto">
  <name>Task 2: «KDV Dahil» label on ProductCard + ProductDetail</name>
  <files>src/components/ProductCard.tsx, src/components/ProductDetail.tsx</files>
  <read_first>src/components/ProductCard.tsx (lines 120–150 price block), src/components/ProductDetail.tsx (lines 720–745 price area), .planning/phases/05-ui/05-PATTERNS.md (lines 260–314)</read_first>
  <action>
    Per D-01, add a small muted «KDV Dahil» label adjacent to the price. In `ProductCard.tsx`
    (lines 133–145), keep the existing `fmtMoney(product.price, 'TRY', bcp47)` Typography and add a
    second Typography directly below/beside it rendering `t('price.kdvDahil')` (fontSize ~11, color
    `palette.primaryLight`, same font family). Ensure a `const t = useTranslations()` exists in the
    component (add the `useTranslations` import from `next-intl` if missing). In `ProductDetail.tsx`
    (line 735), add the same label Typography immediately after the price `<Typography variant="h1">`
    (fontSize ~12, `palette.primaryLight`, `mt: 0.5`), with the same `t`/import. Do not change the
    price value or any layout beyond adding the label. No hardcoded Turkish — text comes from the
    `price.kdvDahil` key.
  </action>
  <verify>
    <automated>cd /home/lexun/work/puz/ACTR && npx tsc --noEmit && grep -q "price.kdvDahil" src/components/ProductCard.tsx && grep -q "price.kdvDahil" src/components/ProductDetail.tsx</automated>
  </verify>
  <done>`tsc --noEmit` clean; both ProductCard and ProductDetail render `t('price.kdvDahil')` next to the price; no hardcoded Turkish; price value unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Informational «KDV (%20)» line in checkout Order Summary</name>
  <files>src/app/[locale]/checkout/page.tsx</files>
  <read_first>src/app/[locale]/checkout/page.tsx (lines 60–80 style consts, ~195–210 totals, ~925–965 orderSummary JSX), .planning/phases/05-ui/05-PATTERNS.md (lines 158–185)</read_first>
  <action>
    Per D-02, add an informational KDV breakdown row to the Order Summary. Import `kdvFromBrutto`
    from `@/lib/kdv`. Compute `const kdvAmount = kdvFromBrutto(<goods subtotal>)` where the goods
    subtotal is the KDV-inclusive brutto value already shown in the Subtotal row (use the same
    variable that backs the existing Subtotal Typography — confirm by reading the summary block;
    likely `finalTotal` or `subtotal`). Insert a new `<Stack direction="row" justifyContent="space-between">`
    row immediately AFTER the Subtotal row and BEFORE Discount/Shipping, with two Typographies using
    the existing `info` style (line 71) and muted color `c['40']`: left = `t('price.kdvLine')`,
    right = `fmtMoney(kdvAmount, currency)`. Add a code comment: `// informational only — price is
    already KDV-inclusive (D-01/D-02)`. CRITICAL anti-pattern (Pitfall 1): do NOT add `kdvAmount` to
    any total/`finalTotal`/shipping computation — the TOTAL row must be byte-for-byte unchanged. Do
    not alter the existing checkout submit/Stripe flow.
  </action>
  <verify>
    <automated>cd /home/lexun/work/puz/ACTR && npx tsc --noEmit && grep -q "kdvFromBrutto" "src/app/[locale]/checkout/page.tsx" && grep -q "price.kdvLine" "src/app/[locale]/checkout/page.tsx" && grep -q "informational only" "src/app/[locale]/checkout/page.tsx"</automated>
  </verify>
  <done>`tsc --noEmit` clean; checkout summary shows a «KDV (%20)» informational row between Subtotal and Shipping using `kdvFromBrutto`; the order TOTAL is unchanged (KDV not summed); comment documents the informational nature.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client display | KDV amount is derived client-side from already-trusted cart totals; no new input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | KDV display accuracy | accept | Fixed 20% (D-03); ARM has no tax field (verified). If ARM later returns a tax field, revisit (Deferred). Low MVP risk. |
| T-05-05 | Tampering | total integrity (double-count) | mitigate | KDV row is informational; explicit Pitfall-1 guard + comment; verify TOTAL computation untouched |
| T-05-06 | Tampering | npm/pip/cargo installs | accept | No package installs (zero new deps, verified) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npx vitest run kdv` green.
- `npm run build` green; `messages/{en,tr}.json` parse with `price.*` parity.
- `grep` confirms `price.kdvDahil` on both product components and `kdvFromBrutto` + `price.kdvLine` in checkout.
- Manual (per VALIDATION): with demo BFF, add an item and confirm the «KDV (%20)» line = round(subtotal − subtotal/1.20) and the order TOTAL is unchanged.
</verification>

<success_criteria>
- «KDV Dahil» label on ProductCard + ProductDetail prices (D-01).
- Informational «KDV (%20)» line in checkout Order Summary, not added to total (D-02).
- Fixed 20% rate in `kdvFromBrutto` (D-03), unit-tested.
- COMP-01 satisfied.
</success_criteria>

<output>
Create `.planning/phases/05-ui/05-02-SUMMARY.md` when done.
</output>
