---
phase: 05-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/en.json
  - messages/tr.json
  - src/app/[locale]/legal/[slug]/page.tsx
  - src/app/[locale]/legal/legal-page.test.tsx
  - src/components/Footer.tsx
autonomous: true
requirements: [COMP-02]
must_haves:
  truths:
    - "A user can open /[locale]/legal/kvkk, /mesafeli-satis, /iade, /gizlilik, /kullanim-kosullari and see a localized stub page (heading + sections)"
    - "All legal page text comes from next-intl keys in EN and TR (no hardcoded Turkish in JSX)"
    - "The footer shows links to all 5 legal pages on desktop and mobile"
    - "An unknown legal slug returns 404 (notFound)"
  artifacts:
    - path: "src/app/[locale]/legal/[slug]/page.tsx"
      provides: "Dynamic SSG stub page for the 5 legal slugs"
      contains: "generateStaticParams"
    - path: "src/components/Footer.tsx"
      provides: "NAV_COL_LEGAL with 5 legal links"
      contains: "NAV_COL_LEGAL"
    - path: "messages/en.json"
      provides: "legal.* keys (EN base)"
      contains: "legal.kvkk.title"
    - path: "messages/tr.json"
      provides: "legal.* keys (real TR labels)"
      contains: "legal.kvkk.title"
  key_links:
    - from: "src/components/Footer.tsx"
      to: "/legal/<slug>"
      via: "Link from @/i18n/navigation"
      pattern: "/legal/kvkk"
    - from: "src/app/[locale]/legal/[slug]/page.tsx"
      to: "legal.<nsKey>.*"
      via: "useTranslations(`legal.${nsKey}`)"
      pattern: "useTranslations\\(`legal"
---

<objective>
Slice C of Phase 5 compliance UI: ship the 5 TR legal stub pages (D-06, D-07) and wire footer
links to them (D-08, footer half). One dynamic SSG route renders all 5 slugs from next-intl
strings (EN base + real TR), and the footer gains a 3rd "Legal" nav column. All content is
placeholder text per D-07 — real legal copy is deferred to a lawyer.

Purpose: Establish the legal-page routes FIRST so the consent checkboxes in 05-03 link to pages
that already exist, and give the storefront the public compliance pages COMP-02 requires.
Output: `legal/[slug]/page.tsx` (SSG), Footer legal column, `legal.*` i18n keys (EN+TR).
</objective>

## Phase Goal

**As a** Turkish shopper, **I want to** read the store's legal pages (KVKK, distance-sales,
returns, privacy, terms) from the footer, **so that** I can verify the store meets TR e-commerce
rules before I buy.

*(Derived from the Phase 5 ROADMAP goal + locked decisions D-06/D-07/D-08; the ROADMAP `Goal:`
line is descriptive, not a user story.)*

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui/05-CONTEXT.md
@.planning/phases/05-ui/05-RESEARCH.md
@.planning/phases/05-ui/05-PATTERNS.md
@.planning/phases/05-ui/05-VALIDATION.md

# Analog files (read before editing — patterns to copy)
@src/app/[locale]/delivery/page.tsx
@src/components/Footer.tsx
@src/i18n/navigation.ts
@src/lib/theme.ts
</context>

<interface_context>
- **next-intl messages are FLAT dotted keys** (verified): `messages/en.json` is `{ "nav.catalog": "Catalog", ... }`, NOT nested. Append flat keys like `"legal.kvkk.title": "..."`. Current count: 336 keys/file, EN base + real TR, parity required.
- **Scoped namespaces resolve flat keys** (verified): existing code uses `useTranslations('partners.schools')` against flat `partners.shops.*` keys. So `useTranslations('legal.kvkk')` + `t('title')` reads the flat key `legal.kvkk.title`.
- **Slug→key rule (Pitfall 4):** URL slug uses hyphens (`mesafeli-satis`), next-intl namespace key uses underscores (`legal.mesafeli_satis.*`). The page converts via `slug.replace(/-/g, '_')`. Mixing fails silently.
- **Locale-aware Link** is `import { Link } from '@/i18n/navigation'` — NEVER `next/link` (Pitfall 5: bare `/legal/kvkk` 404s without locale prefix).
- **Footer** is `'use client'`, root `const t = useTranslations()`; NAV_COL1/NAV_COL2 at lines 64–77, `ALL_NAV` flat mobile list line 77, desktop columns lines 110–137, `navLinkSx` line 27.
- **delivery/page.tsx** is the structural analog: breadcrumb + h1 + `palette.bgLight` card; `useTranslations('delivery')` line 9.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Append legal.* i18n keys (EN base + real TR)</name>
  <files>messages/en.json, messages/tr.json</files>
  <read_first>messages/en.json (first 20 lines for flat-key shape), .planning/phases/05-ui/05-RESEARCH.md (EN key block lines 376–430)</read_first>
  <action>
    Append FLAT dotted keys (NOT nested objects) to both message files for the 5 legal pages, per
    D-06/D-07. Namespace keys use UNDERSCORES (slug hyphens are URL-only). For each of the 5 keys —
    `legal.kvkk`, `legal.mesafeli_satis`, `legal.iade`, `legal.gizlilik`, `legal.kullanim_kosullari`
    — add: `.navLabel` (short footer label), `.title` (page H1), `.intro` (placeholder lead), and
    section pairs `.s1Title`/`.s1Body` … up to the count in RESEARCH lines 376–430 (kvkk has s1–s4,
    mesafeli_satis s1–s3, iade s1–s3, gizlilik s1–s2, kullanim_kosullari s1–s2). EN: use the exact
    English strings from RESEARCH lines 376–430. TR (messages/tr.json): translate `navLabel`/`title`
    to real Turkish (KDV/KVKK/«mesafeli satış» terminology — e.g. title "KVKK Aydınlatma Metni",
    "Mesafeli Satış Sözleşmesi", "İade ve Cayma Hakkı", "Gizlilik Politikası", "Kullanım Koşulları";
    navLabels short), and keep `intro`/`sNBody` as the placeholder marker per D-07 (stub). Maintain
    strict key parity: every key present in en.json MUST exist in tr.json. Do NOT introduce hyphens
    in any key path (Pitfall 4). Do NOT reformat or reorder existing keys.
  </action>
  <verify>
    <automated>cd /home/lexun/work/puz/ACTR && node -e "const en=require('./messages/en.json'),tr=require('./messages/tr.json');const ek=Object.keys(en).filter(k=>k.startsWith('legal.')),tk=Object.keys(tr).filter(k=>k.startsWith('legal.'));if(ek.length===0)throw new Error('no legal keys');const miss=ek.filter(k=>!(k in tr));if(miss.length)throw new Error('TR parity gap: '+miss);if(!en['legal.kvkk.title']||en['legal.mesafeli_satis.title']===undefined)throw new Error('missing required legal titles');if(ek.some(k=>k.includes('-')))throw new Error('hyphen in key');console.log('legal keys EN/TR parity OK:',ek.length)"</automated>
  </verify>
  <done>Both message files parse as valid JSON; every `legal.*` key exists in EN and TR with identical key sets; required titles for all 5 pages present; no hyphenated key paths.</done>
</task>

<task type="auto">
  <name>Task 2: Create the dynamic legal stub page (SSG) + render test</name>
  <files>src/app/[locale]/legal/[slug]/page.tsx, src/app/[locale]/legal/legal-page.test.tsx</files>
  <read_first>src/app/[locale]/delivery/page.tsx (full — breadcrumb/h1/bgLight card structure + style tokens), .planning/phases/05-ui/05-PATTERNS.md (lines 84–154 legal-page pattern), src/components/__tests__/ProductReviews.test.tsx (RTL + next-intl test harness analog)</read_first>
  <action>
    Create `src/app/[locale]/legal/[slug]/page.tsx` as a `'use client'` component modelled on
    delivery/page.tsx (D-07 stub structure). Define `const LEGAL_SLUGS = ['kvkk','mesafeli-satis',
    'iade','gizlilik','kullanim-kosullari'] as const`. Guard: if `params.slug` is not in LEGAL_SLUGS,
    call `notFound()` from `next/navigation`. Convert slug→key with `params.slug.replace(/-/g,'_')`
    and scope strings via `useTranslations(\`legal.${nsKey}\`)`. Render: breadcrumb (Home link via
    `Link` from `@/i18n/navigation`) + H1 `t('title')` + a `palette.bgLight` rounded card containing
    `t('intro')` and the section pairs (`t('s1Title')`/`t('s1Body')` … iterate the section indices
    that exist for that slug — drive from a small per-slug section-count map or render sN while the
    key resolves; simplest: a `SECTIONS: Record<slug, number>` map). Use the delivery typography
    tokens from PATTERNS lines 148–154. Export `generateStaticParams()` returning the 5 slugs (SSG —
    Pitfall: omitting it forces per-request SSR). Use `palette` from `@/lib/theme`; no hardcoded
    Turkish strings anywhere (D-07). Then create `legal-page.test.tsx`: a vitest + RTL test that
    renders the page for each of the 5 slugs (wrapped in `NextIntlClientProvider` with the EN
    messages, same harness as ProductReviews.test.tsx) and asserts the localized `title` text
    appears; one case asserting an unknown slug triggers notFound (or assert LEGAL_SLUGS membership
    logic). Keep the test resilient — if the provider harness is awkward, assert on the title key
    presence in messages and the LEGAL_SLUGS export instead, but prefer a real render.
  </action>
  <verify>
    <automated>cd /home/lexun/work/puz/ACTR && npx tsc --noEmit && npx vitest run legal-page --passWithNoTests && grep -q "generateStaticParams" "src/app/[locale]/legal/[slug]/page.tsx" && grep -q "replace(/-/g" "src/app/[locale]/legal/[slug]/page.tsx" && grep -q "from '@/i18n/navigation'" "src/app/[locale]/legal/[slug]/page.tsx"</automated>
  </verify>
  <done>`tsc --noEmit` clean; legal-page test passes; page exports `generateStaticParams`; uses underscore namespace conversion and the locale-aware `Link`; no hardcoded Turkish in JSX.</done>
</task>

<task type="auto">
  <name>Task 3: Add NAV_COL_LEGAL footer column (desktop + mobile)</name>
  <files>src/components/Footer.tsx</files>
  <read_first>src/components/Footer.tsx (lines 61–137 — NAV_COL defs + desktop column render + ALL_NAV)</read_first>
  <action>
    In `Footer.tsx`, define `NAV_COL_LEGAL` after NAV_COL2 (line 75) with 5 entries built from the
    new keys (D-08 footer half): `{ label: t('legal.kvkk.navLabel'), href: '/legal/kvkk' }`,
    `legal.mesafeli_satis.navLabel`→`/legal/mesafeli-satis`, `legal.iade.navLabel`→`/legal/iade`,
    `legal.gizlilik.navLabel`→`/legal/gizlilik`, `legal.kullanim_kosullari.navLabel`→
    `/legal/kullanim-kosullari`. Extend the mobile flat list: `const ALL_NAV = [...NAV_COL1,
    ...NAV_COL2, ...NAV_COL_LEGAL]`. Add a 3rd desktop nav column inside the `Box` at lines 110–137
    that maps `NAV_COL_LEGAL` with the same `MuiLink component={Link} … sx={navLinkSx}` pattern as
    the existing columns. Keep using `Link` from `@/i18n/navigation` (already imported, line 4) so
    hrefs are locale-prefixed (Pitfall 5). Do not touch SOCIALS/PAYMENT_ICONS or the bottom row.
  </action>
  <verify>
    <automated>cd /home/lexun/work/puz/ACTR && npx tsc --noEmit && grep -q "NAV_COL_LEGAL" src/components/Footer.tsx && grep -cE "/legal/(kvkk|mesafeli-satis|iade|gizlilik|kullanim-kosullari)" src/components/Footer.tsx | { read n; [ "$n" -ge 5 ]; } && grep -q "NAV_COL_LEGAL\]" src/components/Footer.tsx</automated>
  </verify>
  <done>`tsc --noEmit` clean; `NAV_COL_LEGAL` defined with all 5 hrefs; `ALL_NAV` includes the legal column (mobile); a desktop column renders the legal links via the locale-aware Link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→legal route | `params.slug` is untrusted URL input rendered into a namespace lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `legal/[slug]/page.tsx` slug param | mitigate | Allowlist via `LEGAL_SLUGS.includes(...)` → `notFound()`; slug only used for a static-key lookup, never eval/innerHTML |
| T-05-02 | Information Disclosure | legal stub content | accept | Pages are public static placeholder text, no user data, no auth — by design (D-07) |
| T-05-03 | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (zero new deps, verified RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm run build` emits the legal route(s); `messages/{en,tr}.json` parse with EN/TR `legal.*` parity.
- `npx vitest run legal-page` passes.
- Footer contains all 5 `/legal/*` hrefs (desktop column + mobile ALL_NAV).
</verification>

<success_criteria>
- 5 legal stub pages reachable under `/[locale]/legal/<slug>`, rendered from EN+TR next-intl keys (D-06, D-07).
- Footer links to all 5 pages on desktop and mobile (D-08, footer half).
- Unknown slug 404s; SSG via `generateStaticParams`.
- COMP-02 (legal-pages half) satisfied.
</success_criteria>

<output>
Create `.planning/phases/05-ui/05-01-SUMMARY.md` when done.
</output>
