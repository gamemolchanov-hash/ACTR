# Phase 7: Каталог-данные TR - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-01
**Phase:** 7-tr
**Areas discussed:** Источник товаров и тенант, TRY-цены, Способ провижининга, Граница «готово» (500-скоуп)

---

## Источник товаров и тенант

Первичный multiSelect gray-area вопрос владелец отклонил, попросив сначала уточнить.
Ходом уточнения исключены неверные источники.

| Кандидат-источник | Итог |
|-------------------|------|
| `forza-brava.com/api/docs` | ✗ — это только **пример контракта** ARM Storefront API, не источник данных |
| `betapro_products.json` (autoCRM) | ✗ — «нам не нужен бетапро, в Турции будет другой фулфилмент» |
| AC Bitrix-выгрузки / сайт american-creator | ✗ — не выбрано |
| **Локальный ARM-тенант (`demo`)** | ✓ — «тот тенант локальный у которого установлен модуль ARM» |

**User's choice:** Источник товаров = уже заведённые товары в локальном ARM-тенанте `demo`;
тенант — `demo` («бери пока demo за основу»).
**Notes:** Фулфилмент TR ≠ BetaPro → локально `manual` (реальный TR-фулфилмент — деплой-трек).

---

## TRY-цены

| Option | Description | Selected |
|--------|-------------|----------|
| Конвертация по фикс-курсу | Текущие цены × фикс-курс → TRY (как EU-сид USD→EUR), brutto/KDV-inclusive | |
| Ручные TRY-цены | Осмысленные круглые лиры вручную на каждый товар | |
| (свободный ответ) | «Цены не трогай, это потом разберёмся» | ✓ |

**User's choice:** Цены не трогаем — отложено (владелец, позже).
**Notes:** Ни конвертации, ни ручных цен в этой фазе.

---

## Способ провижининга (изоляция autoCRM)

| Option | Description | Selected |
|--------|-------------|----------|
| Сид-скрипт в репо ACTR | Идемпотентный скрипт через ARM/Directus REST, повторяемо, изоляция autoCRM соблюдена | |
| Разово в Directus UI | Вручную через admin, не повторяемо | |
| (свободный ответ) | «Я сам заведу потом» | ✓ |

**User's choice:** Заведение данных (дистрибьютор/витрина/товары/связки) — владелец сделает сам,
позже. Вне код-скоупа фазы.
**Notes:** Рецепт заведения зафиксирован в canonical refs (TZ.md §6) для владельца.

---

## Граница «готово» (500-скоуп)

| Option | Description | Selected |
|--------|-------------|----------|
| Данные + каталог в ₺ end-to-end | Провижининг + устранить 500 + рендер ₺ + спот-чек | |
| **Только «нет 500 в консоли на /catalog»** | Приёмочный критерий — раздел catalog открывается без 500 | ✓ |

Уточнение (т.к. данные заводит владелец сам):

| Option | Description | Selected |
|--------|-------------|----------|
| ACTR устойчив к ошибкам витрины | ACTR не выдаёт 500 в консоль даже без данных (error-boundary/empty-state) | |
| **Приёмка после наполнения** | Фаза делает код-правки; «нет 500» проверяется на данных владельца | ✓ |
| Сначала найти рут-косс 500 | Отдельный /gsd-debug перед планом | |

**User's choice:** Приёмочный критерий фазы = «нет 500-ошибок в консоли при открытии раздела
catalog»; приёмка **после наполнения** владельцем его TR-данными.
**Notes:** Текущий 500 (нет данных/связки currency→distributor) — ожидаемое состояние до
наполнения, не провал фазы. Graceful-degradation не является целью фазы.

---

## Claude's Discretion

- Точный набор мест инъекции `X-Currency` (SSR `server-api.ts` + при необходимости client
  каталог) и способ реализации — планировщик/research уточняет.
- Приведение стале-комментария `/public/oms/storefront/*` (server-api.ts:8) к arm.

## Deferred Ideas

- Назначение TRY-цен (владелец, позже).
- Фактическое заведение TR-данных в ARM (владелец, вручную, позже).
- Реальный TR-фулфилмент/перевозчик (деплой-трек).
- Graceful degradation каталога при ошибке витрины (отдельный слайс, если понадобится).
- Реальные TR-переводы контента товаров (`arm_product_translations` tr-TR) — контент-задача.
