---
phase: 07-tr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/server-api.ts
  - src/lib/api.ts
  - src/lib/server-api.test.ts
  - src/lib/api.test.ts
  - src/app/api/storefront/__tests__/proxy.test.ts
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    # D-06 — SSR currency plumbing (the main fix)
    - "SSR seam `bffGet()` in src/lib/server-api.ts emits `X-Currency` (value `NEXT_PUBLIC_STOREFRONT_CURRENCY || 'TRY'`) on every SSR catalog fetch — product detail, /categories, and the paginated all-products/sitemap walk all funnel through it (D-06)"
    - "The stale pre-ARM OMS proxy-path reference in the src/lib/server-api.ts header docstring (line ~8) is replaced with the current /public/arm/storefront path — grep for the OMS storefront path returns no output (D-06 secondary)"
    # D-07 — client catalog parity (corrected: today it sends NO currency)
    - "Client catalog fetchers `fetchProducts()` and `fetchCategories()` in src/lib/api.ts send `X-Currency` via the existing `currencyHeader()` helper on the actual /catalog listing path (D-07 — corrected: they send no currency today, so this is NEW behavior, not a verify-only step)"
    - "Proxy `route.ts` continues to forward an inbound `X-Currency` header unchanged to the BFF — regression guard stays green (D-07)"
    - "`X-Currency` value derives from the single canonical source `process.env.NEXT_PUBLIC_STOREFRONT_CURRENCY || 'TRY'` reused across api.ts, money.ts and (new) server-api.ts — no second currency constant is introduced"
  prohibitions:
    # D-01 — hard isolation + data population is owner-owned
    - "Do NOT edit any file outside /home/lexun/work/puz/ACTR (no autoCRM / packs / shared BFF / OMS edits) — contract references are read-only (D-01 hard isolation)"
    - "Do NOT populate ARM data (distributor / storefront / products / arm_storefront_distributors links / prices) in code — data population is owner-owned and manual, out of code scope (D-01)"
    # D-02 — no prices, single-currency
    - "Do NOT assign, convert, or read per-product currency; no fixed-rate conversion; single-currency TRY only, no multi-currency logic (D-02)"
    - "Do NOT introduce a new currency env var or a hardcoded non-TRY currency literal (the USD regression marker) — the `currency-default.test.ts` source-invariant grep gate must stay green"
    # D-03 — fulfillment is deploy-track
    - "Do NOT add or change any TR-fulfillment / carrier code — TR fulfillment is manual locally and a deploy-track concern (D-03)"
    # D-05 — the current 500 is expected; no graceful degradation
    - "Do NOT add graceful-degradation / error-boundary / empty-state UI for the current /catalog 500 — the pre-population 500 is an EXPECTED state, not a phase failure (D-05)"
    - "Do NOT add ACTR-side X-Currency validation/normalization — value trimming/upper-casing and unknown-currency fallback are BFF-owned (hard isolation, D-01); ACTR only *sends* the header"
  artifacts:
    - src/lib/api.test.ts        # NEW — axios-mock unit test asserting X-Currency on /products and /categories
    - src/lib/server-api.ts      # modified — X-Currency in bffGet init.headers + stale-comment fix
    - src/lib/api.ts             # modified — currencyHeader() applied to fetchProducts/fetchCategories
    - src/lib/server-api.test.ts # modified — X-Currency assertions on SSR fetch paths
    - src/app/api/storefront/__tests__/proxy.test.ts # modified — X-Currency forwarding regression guard
  key_links:
    - "`currencyHeader()` (src/lib/api.ts:150-154) is REUSED by fetchProducts/fetchCategories — never duplicated"
    - "`bffGet()`'s shared `init.headers` object (src/lib/server-api.ts:71-77) is the single injection seam covering all three SSR fetchers"
    - "`NEXT_PUBLIC_STOREFRONT_CURRENCY || 'TRY'` is the one canonical currency source of truth (api.ts, money.ts, server-api.ts)"
---

<objective>
Complete the storefront currency plumbing so `X-Currency` is emitted on EVERY catalog code path
— SSR fetchers (`server-api.ts`), the client `/catalog` listing fetchers (`api.ts`), and (verified,
unchanged) the proxy passthrough (`route.ts`). This is the ACTR code slice of `DATA-01`.

Purpose: Once the owner populates the local `demo` ARM tenant with a TRY distributor + storefront +
products + `arm_storefront_distributors` links (D-01, owner-owned, out of code scope), the catalog
must resolve TRY and render ₺ end-to-end. Today the SSR path sends no `X-Currency` (D-06) and — per
RESEARCH, correcting D-07 — the client catalog listing (`fetchProducts`/`fetchCategories`) also sends
NO currency at all; only cart/checkout do. This plan closes both gaps.

Scope fence (visible so nothing is silently dropped):
- Data population (distributor/storefront/products/links/prices) is OWNER-OWNED and manual — NOT in
  code scope (D-01). Prices are NOT touched or converted (D-02). TR fulfillment stays `manual` /
  deploy-track (D-03).
- Acceptance = `/catalog` opens with no 500 in console + renders ₺ (D-04), verified by the OWNER
  AFTER they populate TR data (D-05). The current local 500 is EXPECTED pre-population and is NOT a
  phase failure — do NOT add graceful degradation for it (D-05). RESEARCH empirically confirmed
  (direct `curl` to the BFF with and without `X-Currency: TRY` → identical 500) that the header is
  not the blocker; the 500 is a backend/data gap outside this repo.
- Hard isolation: changes ONLY within `/home/lexun/work/puz/ACTR`. No autoCRM/packs/BFF/OMS edits.

Output: `X-Currency` emitted on all catalog paths, stale OMS docstring fixed, and unit tests proving
the MECHANISM on every path (not the data-dependent live 200, per the Validation Architecture).

Note (planner surfaced): the ROADMAP `**Goal:**` line for Phase 7 is prose, not canonical
user-story form. The Phase Goal below is derived from the project Core Value + the ROADMAP goal (not
fabricated). Run `/gsd mvp-phase 7` if a canonical ROADMAP goal line is later desired — not a blocker
for this narrow, well-diagnosed slice.
</objective>

## Phase Goal (MVP vertical slice)

**As a** shopper in Turkey, **I want to** browse the `/catalog` with product prices resolved and
shown in Turkish lira (₺ / TRY), **so that** I can shop American-Creator products at TRY prices on
the familiar AC design.

> Code slice of this goal: emit `X-Currency: TRY` on every catalog request path so the ARM BFF
> resolves the TRY distributor. The user-visible "no 500 + ₺" outcome is owner-verified AFTER data
> population (D-04/D-05); the automated gate here proves the header is sent on every path.

## Acceptance Criteria (automated, this phase)

- [ ] SSR `bffGet()` sends `X-Currency` on product-detail, categories, and all-products paths.
- [ ] Client `fetchProducts()` / `fetchCategories()` send `X-Currency` via `currencyHeader()`.
- [ ] Proxy `route.ts` still forwards an inbound `X-Currency` unchanged (regression guard green).
- [ ] Stale OMS storefront path reference removed from `server-api.ts`.
- [ ] No new test failures beyond the pre-existing 3 in `server-api.test.ts`; `tsc --noEmit` clean.

## Acceptance Criteria (manual, owner, deferred — D-04/D-05)

- [ ] After the OWNER populates TR data (TZ.md §6), `/catalog` on the ACTR dev server (`:3003`)
      opens with no 500 in the browser console and renders ₺ prices. Owner-verified, not gated here.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tr/07-CONTEXT.md
@.planning/phases/07-tr/07-RESEARCH.md
@.planning/phases/07-tr/07-PATTERNS.md
@.planning/phases/07-tr/07-VALIDATION.md

# Files touched this plan (read the current state before editing)
@src/lib/server-api.ts
@src/lib/api.ts
@src/lib/server-api.test.ts
@src/app/api/storefront/[...path]/route.ts
@src/app/api/storefront/__tests__/proxy.test.ts
@src/lib/currency-default.test.ts
</context>

## Artifacts this phase produces

This phase creates almost no new symbols — it wires an existing header value into existing helpers.

| Artifact | Kind | Notes |
|----------|------|-------|
| `src/lib/api.test.ts` | NEW test file | axios-mock unit test; asserts `X-Currency` on `fetchProducts`→`/products` and `fetchCategories`→`/categories` |
| `STOREFRONT_CURRENCY` const in `src/lib/server-api.ts` | NEW module const | `= process.env.NEXT_PUBLIC_STOREFRONT_CURRENCY || 'TRY'`, alongside `TENANT_ID`/`STOREFRONT_KEY` |
| `X-Currency` on SSR catalog fetches | NEW behavior | emitted from `bffGet()` init.headers (covers all 3 SSR fetchers) |
| `X-Currency` on client `/catalog` fetches | NEW behavior | `fetchProducts`/`fetchCategories` now send it via `currencyHeader()` |

No new runtime dependencies, no new components, no new routes, no new env vars.

<tasks>

<task type="auto">
  <name>Task 1 (T-07-01, Wave 0 / RED): X-Currency assertions across all three catalog paths</name>
  <files>src/lib/api.test.ts, src/lib/server-api.test.ts, src/app/api/storefront/__tests__/proxy.test.ts</files>
  <read_first>
    - src/lib/api.ts — the functions under test: `fetchProducts` (L97-129, GET at L127), `fetchCategories` (L141-145, GET at L143), and the `currencyHeader()` helper (L150-154). Note the `USE_MOCKS` short-circuit (L105, L142) that must be disabled in tests.
    - src/lib/currency-default.test.ts — SCAFFOLD ANALOG for the NEW api.test.ts: `describe/it/expect`, `beforeEach`/`afterEach` env reset (L45-55), and the dynamic `await import('./api')` trick (L60) so `process.env` is read at call time.
    - src/lib/server-api.test.ts — SELF ANALOG: existing fetch-mock idiom `const [url, init] = fetchMock.mock.calls[0] as unknown as [string, RequestInit]` (L32) and the `init.headers` cast (L34).
    - src/lib/server-api.ts — to know the `bffGet()` init shape being asserted (init.headers, L71-77).
    - src/app/api/storefront/__tests__/proxy.test.ts — SELF ANALOG: `makeReq`/`makeCtx`/`mockFetch` harness (L13-47) and the `?lang` injection cases (L49-112) to mirror for the `X-Currency` forwarding case.
    - src/app/api/storefront/[...path]/route.ts — confirm it already forwards inbound `x-currency` (L50-51); the proxy case is a regression guard, not new route code.
    - .planning/phases/07-tr/07-PATTERNS.md — the exact NEW-file scaffold for `api.test.ts` (§ "src/lib/api.test.ts (NEW)", including the `vi.mock('axios')` factory and the `USE_MOCKS` guardrail) and the proxy `X-Currency` case pattern.
    - .planning/phases/07-tr/07-VALIDATION.md — the per-task verification map rows (align to DATA-01 unit rows) and the recorded baseline.
  </read_first>
  <behavior>
    - api.test.ts: `fetchProducts()` calls axios GET `/products` with a headers object containing `X-Currency: 'TRY'`. RED before Task 2 (fetchProducts sends no header today).
    - api.test.ts: `fetchCategories()` calls axios GET `/categories` with a headers object containing `X-Currency: 'TRY'`. RED before Task 2.
    - server-api.test.ts: each of the three SSR fetchers passes `init.headers['X-Currency'] === 'TRY'` to fetch. RED before Task 2.
    - proxy.test.ts: an inbound request with header `x-currency: TRY` results in the upstream fetch `init.headers['X-Currency']` being `'TRY'`; an inbound request WITHOUT `x-currency` results in no `X-Currency` on the upstream call. GREEN already (route forwards today) — this is a regression guard.
  </behavior>
  <action>
    Create the NEW file src/lib/api.test.ts using the axios-mock scaffold from 07-PATTERNS.md: mock the `axios` module with a `vi.mock('axios')` factory whose `create()` returns an object with `get`/`post` spies, import `fetchProducts`/`fetchCategories` via dynamic `await import('./api')`, and assert each spy was called with the endpoint path and an options object whose `headers` contains key `X-Currency` equal to the value `TRY`. In `beforeEach`, `vi.clearAllMocks()`, give `get` a default resolved value shaped like the paginated/categories responses, and explicitly `delete process.env.NEXT_PUBLIC_USE_MOCKS` (and leave `NEXT_PUBLIC_STOREFRONT_CURRENCY` unset so the default `TRY` applies) — otherwise the `USE_MOCKS` branch short-circuits the axios call and produces a false pass. Do NOT re-test `currencyHeader()` itself (currency-default.test.ts already owns that).

    Extend src/lib/server-api.test.ts: add NEW `it()` assertions (do NOT modify the 3 pre-existing failing tests) that check the header key `X-Currency` equals `TRY` in `init.headers`, using the existing `fetchMock.mock.calls[0]` destructure idiom. Prefer the `fetchCategoriesServer` happy path (it resolves cleanly via armToCategory) as the primary shared-`bffGet` assertion. Also add a `fetchProductServer` and a `fetchAllProductsServer` header assertion; because `armToProduct` on the existing flat fixtures throws (pre-existing, unrelated — Pitfall 3), either supply a nested `{ product: { name: ... } }` fixture so the call resolves, OR await inside a try/catch and then assert on `fetchMock.mock.calls[0][1].headers['X-Currency']` (fetch is invoked before the adapter runs, so the captured `init` is valid regardless of the adapter throw). Do NOT repair the 3 broken fixtures and do NOT touch the stale URL assertion at L33 (leaving them untouched keeps them masked; repairing is optional stretch, out of this task).

    Extend src/app/api/storefront/__tests__/proxy.test.ts: add a new `describe`/`it` block mirroring the `?lang` cases but asserting on request headers via `mockFetch.mock.calls[0][1]` (the `init` object) instead of the URL. Case 1: a `NextRequest` built with an inbound `x-currency: TRY` header (construct a raw `NextRequest` inline with a `headers` object — `makeReq` does not accept custom headers) → `init.headers['X-Currency']` is `TRY`. Case 2: an inbound request with no currency header → `init.headers['X-Currency']` is undefined. Reuse the existing `mockFetch`/`makeCtx` harness verbatim; do NOT change the harness signature or the proxy route code.

    This task authors tests only — no production code changes. Expect the api.ts and server-api SSR currency assertions to be RED (they exercise behavior added in Task 2); the proxy forwarding case is GREEN immediately (regression guard).
  </action>
  <verify>
    <automated>test -f src/lib/api.test.ts && npx vitest run src/app/api/storefront/__tests__/proxy.test.ts -t "X-Currency"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/api.test.ts` exits 0 (new file exists).
    - `grep -n "X-Currency" src/lib/api.test.ts` shows assertions for both `/products` and `/categories`.
    - `grep -n "X-Currency" src/lib/server-api.test.ts` shows new assertions in the SSR fetch tests.
    - `npx vitest run src/app/api/storefront/__tests__/proxy.test.ts -t "X-Currency"` exits 0 (regression-guard case passes — route already forwards).
    - RED baseline captured: `npx vitest run src/lib/api.test.ts` currently FAILS the two header assertions (no implementation yet) — this is the expected pre-Task-2 state, not a defect.
    - No production source file (`src/lib/api.ts`, `src/lib/server-api.ts`, `route.ts`) was modified in this task.
  </acceptance_criteria>
  <done>
    All three test files contain `X-Currency` assertions on their respective catalog paths; the proxy
    forwarding case passes; the api.ts + SSR assertions are RED pending Task 2; no production code
    touched; the 3 pre-existing `server-api.test.ts` failures are left untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2 (T-07-02, GREEN): emit X-Currency on SSR + client catalog fetchers; fix stale docstring</name>
  <files>src/lib/server-api.ts, src/lib/api.ts</files>
  <read_first>
    - src/lib/server-api.ts — SELF/analog: module constants block (L30-32, `TENANT_ID`/`STOREFRONT_KEY`/`STOREFRONT_BASE`), the `bffGet()` shared `init.headers` seam (L71-77), and the stale header docstring (line ~8). Do NOT touch the 404→null vs 5xx→throw logic (L83-88, load-bearing FBG-67).
    - src/lib/api.ts — SELF/analog: `currencyHeader()` (L150-154) and its existing correct usage in `validatePromo` (L181-185) as the exact `{ headers: currencyHeader() }` shape to mirror; the two target call sites `fetchProducts` GET (L127) and `fetchCategories` GET (L143).
    - .planning/phases/07-tr/07-PATTERNS.md — Pattern 1 (client `currencyHeader()` reuse, merge with existing `params`) and Pattern 2 (server `STOREFRONT_CURRENCY` const + `bffGet` init.headers) with exact before/after seams.
    - .planning/phases/07-tr/07-RESEARCH.md — Architecture Patterns Pattern 1/2 and Anti-Patterns (no second currency literal; do not touch the 4 checkout call sites; no graceful degradation).
    - src/lib/api.test.ts and src/lib/server-api.test.ts — the RED tests authored in Task 1 that this task must turn GREEN. Do NOT create new test files here.
    - src/lib/currency-default.test.ts — the source-invariant grep gate (no `USD` currency literal in `src/`) that must stay green after the edits.
  </read_first>
  <behavior>
    - After this task, `npx vitest run src/lib/api.test.ts` is GREEN (both `/products` and `/categories` send `X-Currency: TRY`).
    - `npx vitest run src/lib/server-api.test.ts -t "X-Currency"` is GREEN (all SSR fetchers send the header).
    - Proxy X-Currency regression guard stays GREEN.
    - `npm test` shows no NEW failures beyond the pre-existing 3 in `server-api.test.ts`; `tsc --noEmit` clean.
  </behavior>
  <action>
    In src/lib/server-api.ts: add a module-level constant `STOREFRONT_CURRENCY` set to
    `process.env.NEXT_PUBLIC_STOREFRONT_CURRENCY || 'TRY'`, placed alongside the existing `TENANT_ID`
    / `STOREFRONT_KEY` constants (~L30-32) — reuse the exact fallback expression already used in
    api.ts and money.ts; do NOT invent a new currency literal or a new env var. Then add the key
    `X-Currency` set to `STOREFRONT_CURRENCY` into the shared `init.headers` object inside `bffGet()`
    (L72-75), next to `X-Tenant-ID` and the conditional `X-Storefront-Key`. This single edit covers
    all three exported SSR fetchers (`fetchProductServer`, `fetchCategoriesServer`,
    `fetchAllProductsServer`) since they all funnel through `bffGet()` — do NOT thread a currency arg
    through each function (per D-06). Also fix the stale pre-ARM proxy-path reference in the file
    header docstring (line ~8): change the mirrored proxy path from the old OMS storefront path to the
    current `/public/arm/storefront/*` path (the `STOREFRONT_BASE` constant at L32 already uses arm;
    only the docstring is stale). Do NOT alter the 404/5xx error-handling logic.

    In src/lib/api.ts: apply the existing `currencyHeader()` helper to the two catalog GET call sites,
    mirroring the `{ headers: currencyHeader() }` shape from `validatePromo`. For `fetchProducts`
    (GET `/products`, L127) merge a `headers: currencyHeader()` entry into the existing axios options
    object WITHOUT removing the existing `params` option. For `fetchCategories` (GET `/categories`,
    L143) add an options object with `headers: currencyHeader()`. Reuse the helper verbatim — do NOT
    duplicate it or hardcode the currency. Do NOT touch the 4 existing checkout call sites
    (`validatePromo` L184, `validateCart` L204, `fetchShippingRates` L232, `createOrder` L275), the
    `USE_MOCKS` branches, or `bearerHeader()` merge logic.

    Do NOT add any error-boundary / empty-state / graceful-degradation for the current 500 (D-05), and
    do NOT add client-side currency validation/normalization (BFF-owned, D-01).
  </action>
  <verify>
    <automated>npx vitest run src/lib/api.test.ts src/lib/server-api.test.ts src/app/api/storefront/__tests__/proxy.test.ts src/lib/currency-default.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "X-Currency" src/lib/server-api.ts` shows the header added in `bffGet()`'s init.headers.
    - `grep -c "currencyHeader()" src/lib/api.ts` returns 6 or more (was 5: 1 def + 4 checkout uses; now +2 catalog uses).
    - `npx vitest run src/lib/api.test.ts` exits 0 (client catalog header assertions GREEN).
    - `npx vitest run src/lib/server-api.test.ts -t "X-Currency"` exits 0 (SSR header assertions GREEN).
    - `npx vitest run src/app/api/storefront/__tests__/proxy.test.ts -t "X-Currency"` exits 0 (regression guard stays GREEN).
    - `grep -n "oms/storefront" src/lib/server-api.ts` returns no output (stale docstring fixed).
    - `npx vitest run src/lib/currency-default.test.ts` exits 0 (no new USD currency literal; TRY source-invariant holds).
    - `npm test` shows no NEW failures beyond the pre-existing 3 in `src/lib/server-api.test.ts`; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `X-Currency` is emitted on every SSR and client catalog fetch path; the stale OMS docstring is
    gone; all new header tests are GREEN; the currency source-invariant holds; the full suite shows
    only the 3 pre-existing `server-api.test.ts` failures and `tsc` is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → Next.js proxy (`route.ts`) | Client-supplied headers (incl. any inbound `X-Currency`) cross here; `X-Storefront-Key` is injected server-side and never enters the client bundle |
| Next.js proxy / SSR fetcher → ARM BFF | Server-to-server call over `BFF_INTERNAL_URL`; BFF validates `X-Storefront-Key` and normalizes/falls back on `X-Currency` |

This phase introduces **no new threat surface**: it only adds an outbound `X-Currency` header with a
build-time-controlled value (`NEXT_PUBLIC_STOREFRONT_CURRENCY || 'TRY'`) to existing, already
authenticated requests. No package installs occur → supply-chain / package-legitimacy checkpoint is
not applicable (no `T-07-SC`).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | Client-supplied `X-Currency` forwarded by `route.ts` to the BFF | low | accept | Already mitigated UPSTREAM (out of ACTR scope, D-01): the BFF trims/upper-cases the value and safely falls back on unknown currencies (`distributor-resolver.ts`, read-only reference). Single-currency (TRY) storefront by design. ACTR's own emitted value is a fixed env-driven constant, not reflected user input. Do NOT add ACTR-side currency validation — that is BFF-owned logic (hard isolation, D-01). |
| T-07-02 | Information Disclosure | `X-Storefront-Key` server-side secret | low | accept | Unchanged by this phase — key is injected only in `route.ts` (proxy) and `server-api.ts` from non-`NEXT_PUBLIC` env; never added to any client-emitted header. No new exposure introduced. |

No `high`-severity threats. ASVS L1: V5 (input validation) is marginal and already satisfied upstream;
no new auth/session/crypto surface is touched.
</threat_model>

<verification>
Per 07-VALIDATION.md (Nyquist strategy). The acceptance ("no 500 on /catalog") depends on
owner-populated data that does not exist locally (D-05), so automated verification proves the
MECHANISM (header emitted on every path), not the data-dependent live 200.

Per-task commit: targeted `npx vitest run <changed test file>`.

Per-wave / phase gate:
```
npm test                 # expect ONLY the 3 pre-existing server-api.test.ts failures; no NEW failures
npx tsc --noEmit         # exit 0 (clean)
npx vitest run src/lib/api.test.ts src/lib/server-api.test.ts src/app/api/storefront/__tests__/proxy.test.ts src/lib/currency-default.test.ts
grep -n "oms/storefront" src/lib/server-api.ts   # expect NO output (stale comment fixed)
```

Baseline (RESEARCH/VALIDATION, before any Phase 7 edit): `npm test` → 16/17 files, 129/132 tests
pass; 3 failures all pre-existing in `server-api.test.ts` (`armToProduct`/fixture mismatch). Do NOT
require 132/132 — that baseline was never green (Open Question #1: fixture repair is optional stretch,
not in this plan).

<human-check>
Owner-only, deferred (D-04/D-05): after the owner populates TR data in the local `demo` ARM tenant
per TZ.md §6 (distributor `currency=TRY` + storefront + products + `arm_storefront_distributors`
`is_default`), load `/catalog` on the ACTR dev server (`:3003`) and confirm no 500 in the browser
console and ₺ prices render. This is NOT an automated gate for this phase and does NOT block
autonomous execution — the local 500 before population is expected.
</human-check>
</verification>

<success_criteria>
- `X-Currency` (`NEXT_PUBLIC_STOREFRONT_CURRENCY || 'TRY'`) is emitted on all three SSR fetchers via
  `bffGet()` and on the client `fetchProducts`/`fetchCategories` via `currencyHeader()` (D-06, D-07).
- Proxy `route.ts` still forwards inbound `X-Currency` unchanged (regression guard green).
- Stale OMS storefront path reference removed from `server-api.ts` (D-06 secondary).
- New unit tests (`api.test.ts` new; `server-api.test.ts`/`proxy.test.ts` extended) are green; full
  suite shows no NEW failures beyond the pre-existing 3; `tsc --noEmit` clean.
- No prices touched, no data populated in code, no autoCRM/packs/BFF edits, no graceful-degradation
  added (D-01/D-02/D-03/D-05 honored).
</success_criteria>

<output>
Create `.planning/phases/07-tr/07-01-SUMMARY.md` when done.
</output>
<!-- planner-discipline-allow: oms/storefront -->
