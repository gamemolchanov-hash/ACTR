---
phase: 06-oms-tr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/page.tsx
  - src/app/[locale]/catalog/page.tsx
  - src/app/[locale]/catalog/[slug]/page.tsx
  - src/components/ProductCard.tsx
  - src/lib/api.ts
  - src/lib/arm-adapter.ts
  - src/features/promo-bogo/
  - public/promo-bogo/
  - messages/en.json
  - messages/tr.json
autonomous: true
requirements: [CLEAN-01]

must_haves:
  truths:
    - "D-08 / CLEAN-01: no BOGO auto-promo code, markers, imports, `active_promo` field, or `promo.*` i18n keys remain anywhere in src/public"
    - "Live-guard: `validatePromo`, `PromoValidationResult`, `promoCode` (CART-06) are UNTOUCHED and still present — deleting BOGO must not touch the live promo-CODE feature"
    - "`npx tsc --noEmit` stays clean; no dangling `@/features/promo-bogo` import survives"
    - "messages/en.json and messages/tr.json keep identical key sets after `promo.*` removal (EN/TR parity)"
  artifacts:
    - path: "src/lib/api.ts"
      provides: "Product type + live promo-CODE API (validatePromo/PromoValidationResult) — active_promo field removed, live promo kept"
      contains: "validatePromo"
    - path: "src/lib/arm-adapter.ts"
      provides: "ARM→Product adapter with `active_promo: null` line removed"
      contains: "PromoValidationResult"
  key_links:
    - from: "src/components/ProductCard.tsx"
      to: "removed"
      via: "PromoBadge import deleted (BOGO HOOK block)"
      pattern: "PromoBadge"
    - from: "messages/en.json"
      to: "messages/tr.json"
      via: "identical key set (parity) after promo.* removal"
      pattern: "\"promo\\."
---

<objective>
Remove the dead OMS-inherited BOGO auto-promo feature (D-08, CLEAN-01): the entire
`src/features/promo-bogo/` module, its public assets, every consumer site (marker-delimited
`// BOGO HOOK` blocks in 4 files + the `active_promo` data field in 2 lib files), and the 3
`promo.*` i18n keys in both locales.

Purpose: The BOGO campaign was a time-boxed May-2026 RU promo (expired per its own
`config.ts` `PROMO_TO_ISO`); ARM does not serve auto-promos. This is the first slice of the
"clean TR store" phase — after it, no Russian-market gift-promo banner or badge renders anywhere.
Output: A storefront with zero BOGO code/assets/keys, live promo-CODE feature (CART-06) intact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-oms-tr/06-RESEARCH.md
@.planning/phases/06-oms-tr/06-PATTERNS.md
@.planning/phases/06-oms-tr/06-VALIDATION.md
@src/features/promo-bogo/README.md
</context>

## Phase Goal

**As a** Turkish shopper, **I want to** browse a storefront with no leftover Russian-market
features (auto-promo banners, non-functional reviews, RU recruitment pages, CDEK delivery copy,
RU contacts and payment methods), **so that** the shop reads as a genuine Turkish store I can trust.

> NOTE (planner): the ROADMAP Phase-6 `**Goal**` line is stated as an outcome
> ("Удалён мёртвый OMS-код, бренд/контент адаптированы под TR"), not in `As a / I want / so that`
> form. The story above is the faithful user-facing restatement of that goal + the project Core
> Value; run `/gsd mvp-phase 6` if you want it formalized in ROADMAP.

<tasks>

<task type="auto">
  <name>Task 1: Unwire all BOGO consumer sites (remove imports, JSX, and active_promo field)</name>
  <files>src/app/[locale]/page.tsx, src/app/[locale]/catalog/page.tsx, src/app/[locale]/catalog/[slug]/page.tsx, src/components/ProductCard.tsx, src/lib/api.ts, src/lib/arm-adapter.ts</files>
  <read_first>
    - src/features/promo-bogo/README.md (self-documented removal procedure + BOGO HOOK convention)
    - 06-RESEARCH.md → "Pattern 1: Marker-delimited removal (BOGO)" (exhaustive verified site list)
    - 06-PATTERNS.md → "Marker-delimited BOGO removal" + "Isolation guard — do not touch live promo-CODE feature"
    - Each target file's BOGO HOOK region before editing
  </read_first>
  <action>
    Remove every `// BOGO HOOK START` … `// BOGO HOOK END` block (and the JSX-comment form
    `{/* BOGO HOOK START */}` … `{/* BOGO HOOK END */}`) at these exact sites, deleting the wrapped
    import/JSX in each:
    - src/app/[locale]/page.tsx:5-7 (import `PromoBanner`) and :12-14 (`<PromoBanner />`)
    - src/app/[locale]/catalog/page.tsx:5-7 and :12-14
    - src/app/[locale]/catalog/[slug]/page.tsx:6-8 and :14-16
    - src/components/ProductCard.tsx:13-15 (import `PromoBadge`) and :43-45 (`<PromoBadge product={product} />`)
    In src/lib/api.ts remove the `active_promo` field on the Product type: delete lines 44-46
    (the `// BOGO HOOK START` comment, the `active_promo?: {...} | null;` field at :45, and the
    `// BOGO HOOK END` comment). In src/lib/arm-adapter.ts remove the single line 54
    `active_promo: null, // OMS BOGO promos are not supported in ARM`.
    ISOLATION GUARD (per D-08 naming trap): do NOT touch `validatePromo`, `PromoValidationResult`,
    `armToPromoResult`, or `promoCode` in api.ts/arm-adapter.ts — those are the live CART-06
    promo-CODE feature. Only `active_promo` and `BOGO HOOK`-marked code is in scope.
    Do NOT delete the `src/features/promo-bogo/` directory yet (Task 2) — after this task the module
    is orphaned/unused, which is tsc-clean.
  </action>
  <verify>
    <automated>git grep -n "BOGO HOOK\|PromoBanner\|PromoBadge\|active_promo" -- src/app src/components src/lib | grep -v "features/promo-bogo" | wc -l | grep -qx 0 && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "BOGO HOOK\|PromoBanner\|PromoBadge\|active_promo" -- src/app src/components src/lib` (excluding `src/features/promo-bogo/`) → 0 matches
    - `git grep -n "validatePromo\|PromoValidationResult\|promoCode" -- src/lib/api.ts src/lib/arm-adapter.ts src/app/[locale]/basket/page.tsx` → still > 0 (live guard intact)
    - `npx tsc --noEmit` → 0 errors (no dangling import)
  </acceptance_criteria>
  <done>All 8 BOGO HOOK sites + active_promo field/line removed; live promo-CODE feature untouched; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete promo-bogo module + assets and purge promo.* i18n keys</name>
  <files>src/features/promo-bogo/, public/promo-bogo/, messages/en.json, messages/tr.json</files>
  <read_first>
    - 06-VALIDATION.md → "Absence-Grep Gates" (BOGO gone) + "i18n Parity Gate"
    - 06-PATTERNS.md → "i18n key removal — flat-dotted parity"
    - messages/en.json and messages/tr.json (locate keys `promo.gift`, `promo.giftAdd`, `promo.bannerAlt`)
  </read_first>
  <action>
    Delete the entire directory `src/features/promo-bogo/` (config.ts, PromoBanner.tsx,
    PromoBadge.tsx, PromoPlashka.tsx, useAutoPromo.ts, index.ts, README.md) — verified orphaned after
    Task 1 (`useAutoPromo`/`PromoPlashka` were never imported by the app). Delete the public asset
    directory `public/promo-bogo/` (banner-desktop.png, banner-mobile.png).
    In messages/en.json AND messages/tr.json remove exactly these 3 flat-dotted keys from EACH file:
    `promo.gift`, `promo.giftAdd`, `promo.bannerAlt`. Keep every other key. Maintain EN/TR parity
    (both files must end with an identical key set: 385 keys each, down from 388).
  </action>
  <verify>
    <automated>git grep -n "promo-bogo\|BOGO HOOK\|active_promo" -- src public | wc -l | grep -qx 0 && python3 -c "import json; en=json.load(open('messages/en.json')); tr=json.load(open('messages/tr.json')); assert set(en)==set(tr), set(en)^set(tr); assert not any(k.startswith('promo.') for k in en), 'promo.* survived'; print('parity',len(en))" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "promo-bogo\|BOGO HOOK\|active_promo" -- src public` → 0 matches
    - `ls src/features/promo-bogo public/promo-bogo` → both absent
    - No `promo.*` key remains in either messages file; `set(en)==set(tr)`; 385 keys each
    - No surviving src file references `promo.gift|promo.giftAdd|promo.bannerAlt`:
      `git grep -n "promo.gift\|promo.giftAdd\|promo.bannerAlt" -- src` → 0
    - `npx tsc --noEmit` → 0 errors
  </acceptance_criteria>
  <done>promo-bogo module + assets gone; promo.* keys purged in both locales with parity held; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client render → i18n dictionary | pages read i18n keys; a removed-but-still-referenced key degrades silently to a literal string (availability/defacement risk, not a crash) |
| BOGO cleanup ↔ live promo-CODE (CART-06) | adjacent code in api.ts/arm-adapter.ts sharing the word "promo"; deleting the wrong one breaks a live, required checkout feature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | src/lib/api.ts, src/lib/arm-adapter.ts | mitigate | Only remove `active_promo` + `BOGO HOOK`-marked code; grep-guard `validatePromo`/`PromoValidationResult`/`promoCode` still present after each commit; `tsc --noEmit` must stay clean |
| T-06-02 | Denial of Service (render) | messages/{en,tr}.json + [locale] pages | mitigate | After removing `promo.*` keys, grep surviving `src/**` for the removed prefixes (expect 0); `tsc`/build green proves no dangling `@/features/promo-bogo` import |
| T-06-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | This plan installs no packages (RESEARCH "Package Legitimacy Audit: N/A"); no registry surface introduced |

Block-on-high: no `high`-severity threat introduced (net attack-surface reduction — one fewer client→BFF code path).
</threat_model>

## Artifacts this phase produces

Deletion slice — the only positive artifacts are the surviving/edited files (no new symbols):
- EDITED `src/lib/api.ts` — Product type without `active_promo`; live `validatePromo`/`PromoValidationResult`/`promoCode` retained (no signature change).
- EDITED `src/lib/arm-adapter.ts` — adapter without the `active_promo: null` line.
- REMOVED: `src/features/promo-bogo/**`, `public/promo-bogo/**`, i18n keys `promo.gift`/`promo.giftAdd`/`promo.bannerAlt` (both locales).
- No new files, no new exported symbols created by this plan.

<verification>
- `git grep -n "BOGO HOOK\|promo-bogo\|active_promo" -- src public` → 0 (VALIDATION absence-gate: BOGO gone)
- `git grep -n "validatePromo\|PromoValidationResult\|promoCode" -- src` → > 0 (VALIDATION live-code guard)
- i18n parity script green (385/385, no `promo.*`)
- `npx tsc --noEmit` clean; `npx vitest run` shows no NEW failures vs 141/144 baseline (3 pre-existing `server-api.test.ts` failures only)
</verification>

<success_criteria>
- CLEAN-01 (BOGO portion) satisfied: BOGO module, consumers, assets, and i18n keys fully removed
- D-08 honored exactly; live CART-06 promo-CODE feature demonstrably untouched
- EN/TR i18n parity preserved; tsc clean; no new vitest failures
</success_criteria>

<output>
Create `.planning/phases/06-oms-tr/06-01-SUMMARY.md` when done.
</output>
