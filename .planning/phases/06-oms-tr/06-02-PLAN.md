---
phase: 06-oms-tr
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/ProductDetail.tsx
  - src/app/[locale]/catalog/[slug]/[productSlug]/page.tsx
  - src/lib/seo.ts
  - src/lib/seo.test.ts
  - src/lib/server-api.ts
  - src/lib/api.ts
  - src/components/__tests__/ProductDetail.sanitize.test.tsx
  - src/components/ProductReviews.tsx
  - src/components/__tests__/ProductReviews.test.tsx
  - public/robots.txt
  - messages/en.json
  - messages/tr.json
autonomous: true
requirements: [CLEAN-01, CLEAN-02]

must_haves:
  truths:
    - "D-09 / CLEAN-01: ProductReviews component, its test, all review client calls (fetchProductReviews/submitReview), the ReviewAggregate type, fetchProductReviewAggregateServer, and the JSON-LD aggregateRating block are removed; ARM has no /reviews contract (confirmed)"
    - "D-11 / CLEAN-02: public/robots.txt no longer hardcodes american-creator.ru; seo.test.ts:240 assertion updated to match, test green"
    - "product.review* i18n keys (13) removed in both locales with EN/TR parity held"
    - "npx vitest run has no NEW failures vs baseline; the 11 ProductReviews tests are gone, the 3 pre-existing server-api failures remain"
    - "Live-guard: validatePromo/PromoValidationResult/promoCode untouched (adjacent in api.ts)"
  artifacts:
    - path: "src/lib/seo.ts"
      provides: "buildProductJsonLd WITHOUT reviews param / aggregateRating (star-snippet block dropped)"
      contains: "buildProductJsonLd"
    - path: "public/robots.txt"
      provides: "robots.txt with de-RU-ified Sitemap directive (placeholder/relative)"
      contains: "Sitemap:"
  key_links:
    - from: "src/components/ProductDetail.tsx"
      to: "removed"
      via: "ProductReviews import + <ProductReviews/> usage deleted"
      pattern: "ProductReviews"
    - from: "src/app/[locale]/catalog/[slug]/[productSlug]/page.tsx"
      to: "src/lib/seo.ts buildProductJsonLd"
      via: "call updated — reviews fetch + 2nd JSON-LD arg removed"
      pattern: "buildProductJsonLd"
---

<objective>
Remove the dead product-reviews feature (D-09, CLEAN-01) — ARM never served reviews (verified: no
`/reviews` path in ARM OpenAPI; `fetchProductReviewAggregateServer` is already a `return null` stub
commented "cleanup in Phase 6"). Delete the component, its test, all client review calls, the
`ReviewAggregate` type + `aggregateRating` JSON-LD block, and the 13 `product.review*` i18n keys.
Also de-RU the one remaining hardcoded RU domain in a shipped artifact: `public/robots.txt`'s
`Sitemap: https://american-creator.ru/...` (D-11, CLEAN-02), updating its guarding assertion in
`seo.test.ts`.

Purpose: Second "clean TR store" slice — the product page loses a review section that could never
populate (removing zero-count `aggregateRating` is also a minor SEO improvement), and no shipped
file advertises the old RU domain.
Output: Product pages with no reviews UI/JSON-LD; robots.txt free of the RU domain; suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-oms-tr/06-RESEARCH.md
@.planning/phases/06-oms-tr/06-PATTERNS.md
@.planning/phases/06-oms-tr/06-VALIDATION.md
@src/lib/seo.ts
@src/lib/server-api.ts
</context>

## Phase Goal

**As a** Turkish shopper, **I want to** browse a storefront with no leftover Russian-market
features (auto-promo banners, non-functional reviews, RU recruitment pages, CDEK delivery copy,
RU contacts and payment methods), **so that** the shop reads as a genuine Turkish store I can trust.

<tasks>

<task type="auto">
  <name>Task 1: Unwire reviews from ProductDetail, product page, and SEO builders</name>
  <files>src/components/ProductDetail.tsx, src/app/[locale]/catalog/[slug]/[productSlug]/page.tsx, src/lib/seo.ts, src/lib/seo.test.ts, src/lib/server-api.ts, src/lib/api.ts, src/components/__tests__/ProductDetail.sanitize.test.tsx</files>
  <read_first>
    - 06-RESEARCH.md → "Pattern 2: Contract-verified deletion (Reviews)" (exhaustive consumer graph with line numbers)
    - src/lib/seo.ts (ReviewAggregate interface :143, reviews param :159, aggregateRating block :186-194)
    - src/lib/server-api.ts (import :23, fetchProductReviewAggregateServer :117-129)
    - src/lib/api.ts (ProductReview :152, ProductReviewsResponse :161, EMPTY_REVIEWS :166, fetchProductReviews :171, submitReview :181)
    - src/app/[locale]/catalog/[slug]/[productSlug]/page.tsx (import :6, reviews fetch :44, JSON-LD 2nd arg)
  </read_first>
  <action>
    In src/components/ProductDetail.tsx remove the import at :29 (`import { ProductReviews } from './ProductReviews';`)
    and the usage at :893 (`<ProductReviews productId={product.id} />`).
    In src/app/[locale]/catalog/[slug]/[productSlug]/page.tsx: change the import at :6 to drop
    `fetchProductReviewAggregateServer` (keep `fetchProductServer`); remove the reviews fetch line
    (:44 `const reviews = await fetchProductReviewAggregateServer(product.id);` plus its comment at :42);
    remove the 2nd argument (`reviews`) passed to `buildProductJsonLd`.
    In src/lib/seo.ts remove the `ReviewAggregate` interface (block at :143 incl. its doc comment
    :139-142), remove the `reviews?: ReviewAggregate | null` parameter from `buildProductJsonLd`
    (:159), and remove the `if (reviews && reviews.count > 0 …) { jsonLd.aggregateRating = {…} }`
    block (:186-194) plus the stale doc lines :150-153 referencing reviews.
    In src/lib/server-api.ts remove `import type { ReviewAggregate } from './seo';` (:23) and the
    entire `fetchProductReviewAggregateServer` function (:117-129, incl. its doc comment).
    In src/lib/api.ts remove the reviews block: `ProductReview` interface (:152), `ProductReviewsResponse`
    (:161), `EMPTY_REVIEWS` (:166), `fetchProductReviews` (:171 onward), `submitReview` (:181 onward).
    ISOLATION GUARD: do NOT touch `validatePromo`/`PromoValidationResult`/`promoCode` in api.ts.
    In src/lib/seo.test.ts: delete the test `it('adds aggregateRating when there are approved reviews …')`
    (:187-196) and, in `it('omits aggregateRating without reviews …')` (:198+), remove the two
    assertions that pass a `reviews` 2nd arg to `buildProductJsonLd` — keep the no-arg
    `buildProductJsonLd(baseProduct)` assertion (which must still yield `aggregateRating` undefined).
    In src/components/__tests__/ProductDetail.sanitize.test.tsx remove the now-stale
    `vi.mock('@/lib/auth-context', …)` (~:47-52) and its "ProductDetail embeds ProductReviews, which
    calls useAuth()" comment — after D-09 ProductDetail no longer imports ProductReviews (inert mock;
    delete for cleanliness). Do NOT delete `src/components/ProductReviews.tsx` yet (Task 2).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/seo.test.ts src/components/__tests__/ProductDetail.sanitize.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -ni "aggregateRating\|ReviewAggregate\|fetchProductReviewAggregateServer\|fetchProductReviews\|submitReview" -- src/lib src/components/ProductDetail.tsx src/app` → 0
    - `git grep -n "validatePromo\|PromoValidationResult\|promoCode" -- src/lib/api.ts` → still > 0 (live guard)
    - `npx tsc --noEmit` → 0 errors
    - `npx vitest run src/lib/seo.test.ts` → green (no aggregateRating tests; no-arg omit assertion passes)
  </acceptance_criteria>
  <done>All reviews consumers unwired; seo.ts JSON-LD has no aggregateRating; seo.test.ts trimmed and green; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete ProductReviews files + purge product.review* i18n keys</name>
  <files>src/components/ProductReviews.tsx, src/components/__tests__/ProductReviews.test.tsx, messages/en.json, messages/tr.json</files>
  <read_first>
    - 06-VALIDATION.md → "Absence-Grep Gates" (Reviews gone) + "i18n Parity Gate"
    - messages/en.json (locate the 13 review keys listed in the action below)
    - Baseline: `npx vitest run` = 141/144 (3 pre-existing server-api failures) — do not let these mask new regressions
  </read_first>
  <action>
    Delete `src/components/ProductReviews.tsx` and `src/components/__tests__/ProductReviews.test.tsx`
    (verified orphaned after Task 1). In messages/en.json AND messages/tr.json remove exactly these 13
    flat-dotted keys from EACH file: `product.reviews`, `product.noReviews`, `product.yourRating`,
    `product.ratingAriaLabel`, `product.sharePlaceholder`, `product.submitReview`, `product.submitting`,
    `product.verifiedPurchase`, `product.loginPromptText`, `product.loginLink`, `product.reviewCount`,
    `product.customer`, `product.sendError`. Keep all other `product.*` keys. Maintain EN/TR parity
    (372 keys each after this plan's removals: 385 from Plan 01 − 13).
  </action>
  <verify>
    <automated>[ "$(git grep -ni "ProductReviews\|product.reviews\|product.yourRating\|product.submitReview\|product.verifiedPurchase" -- src | wc -l)" = "0" ] && python3 -c "import json; en=json.load(open('messages/en.json')); tr=json.load(open('messages/tr.json')); rev={'product.reviews','product.noReviews','product.yourRating','product.ratingAriaLabel','product.sharePlaceholder','product.submitReview','product.submitting','product.verifiedPurchase','product.loginPromptText','product.loginLink','product.reviewCount','product.customer','product.sendError'}; assert set(en)==set(tr); assert not (rev & set(en)); print('parity',len(en))" && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -ni "ProductReviews" -- src` → 0; `ls src/components/ProductReviews.tsx src/components/__tests__/ProductReviews.test.tsx` → both absent
    - No `product.review*` key remains in either messages file; `set(en)==set(tr)`; 372 keys each
    - No surviving src references a removed key: `git grep -n "product.reviews\|product.noReviews\|product.yourRating\|product.submitReview\|product.verifiedPurchase" -- src` → 0
    - `npx vitest run` → total drops to 133 tests (144 − 11 ProductReviews), still exactly 3 pre-existing failures, no NEW failures
  </acceptance_criteria>
  <done>ProductReviews files deleted; product.review* keys purged both locales with parity; no new vitest failures.</done>
</task>

<task type="auto">
  <name>Task 3: De-RU robots.txt Sitemap domain (D-11) + update its guarding assertion</name>
  <files>public/robots.txt, src/lib/seo.test.ts</files>
  <read_first>
    - 06-CONTEXT.md → D-11 (RU domain in robots.txt → TR placeholder/relative path)
    - 06-RESEARCH.md → "Open Questions" #1 (recommend relative/env-consistent placeholder, update seo.test.ts:240 same commit)
    - public/robots.txt (current `Sitemap: https://american-creator.ru/sitemap.xml` on last line)
    - src/lib/seo.test.ts:240 (`expect(robots).toContain('Sitemap: https://american-creator.ru/sitemap.xml');`)
  </read_first>
  <action>
    In public/robots.txt replace the line `Sitemap: https://american-creator.ru/sitemap.xml` with a
    de-RU, domain-agnostic placeholder consistent with the codebase's env-driven `SITE_URL` convention
    (which everywhere else avoids the hardcoded RU domain). Use the relative form `Sitemap: /sitemap.xml`
    (the real absolute `.tr` domain is a deploy-track item, deferred per PROJECT.md). Leave the rest of
    robots.txt (User-agent/Allow/Disallow lines) unchanged.
    In src/lib/seo.test.ts update the assertion at :240 to match the new value:
    `expect(robots).toContain('Sitemap: /sitemap.xml');` (adjust the exact string to whatever value you
    wrote to robots.txt so the test stays truthful). Do not weaken the test to a no-op — it must still
    assert the actual Sitemap line present in robots.txt.
  </action>
  <verify>
    <automated>[ "$(git grep -n "american-creator.ru" -- public/robots.txt | wc -l)" = "0" ] && npx vitest run src/lib/seo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "american-creator.ru" -- public/robots.txt` → 0
    - robots.txt `Sitemap:` line is a de-RU placeholder/relative path
    - `src/lib/seo.test.ts` robots assertion updated to the exact new value; `npx vitest run src/lib/seo.test.ts` → green
  </acceptance_criteria>
  <done>robots.txt no longer carries the RU domain; seo.test.ts asserts the new Sitemap line and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → BFF review endpoints | `fetchProductReviews`/`submitReview` were client calls to endpoints ARM never implemented; deleting them removes a client-triggerable code path (net reduction) |
| product JSON-LD → search engines | `aggregateRating` in structured data was always empty; removing it avoids emitting invalid zero-count structured data |
| shipped static artifact → SEO identity | `public/robots.txt` is the one build-shipped file still naming the RU domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | src/lib/seo.ts (JSON-LD) | mitigate | Remove always-empty `aggregateRating`; no invalid structured data emitted (Google penalizes zero-count) — verified by seo.test.ts no-arg omit assertion |
| T-06-04 | Tampering (input surface) | src/lib/api.ts submitReview() | mitigate | Delete the `submitReview` text/rating input→BFF round trip entirely (ASVS V5 net reduction — one fewer user-input path); do not touch live `validatePromo` |
| T-06-05 | Denial of Service (render) | messages/{en,tr}.json + ProductDetail | mitigate | After removing `product.review*` keys, grep surviving src for the prefixes (expect 0); tsc/build green proves no dangling ProductReviews import |
| T-06-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH audit N/A) |

Block-on-high: no `high`-severity threat; this plan is net attack-surface reducing.
</threat_model>

## Artifacts this phase produces

Deletion + de-RU slice — surviving/edited artifacts (no new exported symbols):
- EDITED `src/lib/seo.ts` — `buildProductJsonLd` single-arg signature (reviews param + `aggregateRating` removed).
- EDITED `src/lib/server-api.ts`, `src/lib/api.ts` — review types/functions removed; live promo-CODE API retained.
- EDITED `public/robots.txt` — `Sitemap:` directive de-RU-ified (relative `/sitemap.xml` placeholder).
- REMOVED: `src/components/ProductReviews.tsx`, `src/components/__tests__/ProductReviews.test.tsx`, 13 `product.review*` keys (both locales).
- No new files or exported symbols created by this plan.

<verification>
- `git grep -ni "ProductReviews\|aggregateRating\|ReviewAggregate" -- src` → 0 (VALIDATION absence-gate: Reviews gone)
- `git grep -n "american-creator.ru" -- public/robots.txt` → 0 (D-11)
- `git grep -n "validatePromo\|PromoValidationResult\|promoCode" -- src` → > 0 (live-code guard)
- i18n parity script green (372/372, no `product.review*`)
- `npx tsc --noEmit` clean; `npx vitest run` → 133 tests, still exactly 3 pre-existing failures, no NEW failures
</verification>

<success_criteria>
- CLEAN-01 (Reviews portion) satisfied: component, tests, client calls, types, JSON-LD block, i18n keys removed
- CLEAN-02 (D-11) satisfied: robots.txt free of RU domain; guarding test updated and green
- D-09 + D-11 honored exactly; live CART-06 promo-CODE feature untouched; EN/TR parity held
</success_criteria>

<output>
Create `.planning/phases/06-oms-tr/06-02-SUMMARY.md` when done.
</output>
