---
phase: 06-oms-tr
plan: 05
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - src/components/Footer.tsx
  - src/components/Header.tsx
  - src/app/[locale]/contacts/page.tsx
  - public/icons/soc-vk.png
  - public/icons/soc-wb.svg
  - public/icons/soc-telegram.png
  - public/icons/soc-instagram.png
  - public/icons/paykeeper.png
  - public/icons/pay-systems.png
  - public/icons/payment.svg
  - public/icons/pay-troy.png
autonomous: true
requirements: [CLEAN-02]

must_haves:
  truths:
    - "D-03 / CLEAN-02: the RU phone +7 995 757-84-67 (79957578467) is replaced by a TR placeholder at ALL 4 sites — Footer.tsx:163,204, Header.tsx:140, contacts/page.tsx tel: link, and the wa.me URL in Footer.tsx:10"
    - "D-10 / CLEAN-02: the RU email info@american-creator.ru in contacts/page.tsx (mailto + text) is replaced by a de-RU TR placeholder email"
    - "D-04 / CLEAN-02: SOCIALS drops VK (vk.com) and Wildberries and the RU Telegram handle americancreator_ru; keeps WhatsApp (TR placeholder number) and adds Instagram (placeholder); orphan RU social assets deleted"
    - "D-05 / CLEAN-02: Footer PAYMENT_ICONS drops yandex_money/webmoney/qiwi and the PayKeeper img; keeps Visa/Mastercard (sprite) and adds Troy via a standalone <img>; RU/orphan payment assets deleted"
    - "Live-guard: KDV, KVKK/mesafeli consent, and /legal/[slug] (Phase 5) are UNTOUCHED"
  artifacts:
    - path: "public/icons/pay-troy.png"
      provides: "Troy (TR national scheme) payment icon placeholder asset"
    - path: "public/icons/soc-instagram.png"
      provides: "Instagram social icon placeholder asset"
    - path: "src/components/Footer.tsx"
      provides: "Footer with TR phone, TR socials (WhatsApp/Instagram), Visa/Mastercard/Troy payment icons"
      contains: "PAYMENT_ICONS"
  key_links:
    - from: "src/components/Footer.tsx"
      to: "public/icons/pay-troy.png"
      via: "standalone <img> Troy payment icon (per PATTERNS analog)"
      pattern: "pay-troy"
    - from: "src/app/[locale]/contacts/page.tsx"
      to: "TR placeholder tel:/mailto:"
      via: "phone + email swapped to TR placeholders"
      pattern: "tel:|mailto:"
---

<objective>
Swap the RU brand-contact identity for TR placeholders (CLEAN-02): the phone number at all 4 sites
(D-03), the RU email (D-10), the social links (D-04 — drop VK/WB/Telegram-RU, keep WhatsApp, add
Instagram), and the Footer payment icons (D-05 — drop yandex_money/webmoney/qiwi + PayKeeper, keep
Visa/Mastercard, add Troy). All values are TR placeholders (real contacts/handles are deploy-track).

Purpose: Final "clean TR store" slice — after it, no VK/WB/Telegram-RU handle, no RU phone/email, no
RU payment scheme (yandex_money/webmoney/qiwi/PayKeeper) remains; the footer reads as a Turkish store
(TR phone, WhatsApp/Instagram, Visa/Mastercard/Troy). KDV/KVKK/legal (Phase 5) stay untouched.
Output: TR-branded Header/Footer/contacts with placeholder contact data + Troy/Instagram assets.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-oms-tr/06-RESEARCH.md
@.planning/phases/06-oms-tr/06-PATTERNS.md
@.planning/phases/06-oms-tr/06-VALIDATION.md
@src/components/Footer.tsx
@src/app/[locale]/contacts/page.tsx
</context>

## Phase Goal

**As a** Turkish shopper, **I want to** browse a storefront with no leftover Russian-market
features (auto-promo banners, non-functional reviews, RU recruitment pages, CDEK delivery copy,
RU contacts and payment methods), **so that** the shop reads as a genuine Turkish store I can trust.

<tasks>

<task type="auto">
  <name>Task 1: Swap phone + email + socials to TR placeholders (D-03/D-10/D-04)</name>
  <files>src/components/Footer.tsx, src/components/Header.tsx, src/app/[locale]/contacts/page.tsx, public/icons/soc-vk.png, public/icons/soc-wb.svg, public/icons/soc-telegram.png, public/icons/soc-instagram.png</files>
  <read_first>
    - 06-CONTEXT.md then D-03 (phone, 3 files + wa.me), D-04 (socials), D-10 (email)
    - 06-PATTERNS.md then "Phone pattern — 3 sites" + "SOCIALS analog" (Footer.tsx:8-17 array, render loop 34-59)
    - src/components/Footer.tsx (SOCIALS :8-17, phone :163/:204, wa.me :10), src/components/Header.tsx (phone :140), src/app/[locale]/contacts/page.tsx (email :80/:92, tel :96/:107)
    - global CLAUDE.md then image-generation rule (~/generate_image.py) for the Instagram icon
  </read_first>
  <action>
    PHONE (D-03) — replace the RU number at all 4 sites with the TR placeholder `+90 500 000 00 00`
    (tel form `+905000000000`): Footer.tsx:163 and :204 (plain Typography display text), Header.tsx:140
    (display text), contacts/page.tsx:107 (display text) AND its `tel:` href at :96 (change
    `tel:+79957578467` to `tel:+905000000000` — preserve the clickable `tel:` link shape), and the
    WhatsApp URL in Footer.tsx:10 (`wa.me/79957578467` to `wa.me/905000000000`).
    EMAIL (D-10) — in contacts/page.tsx replace the RU email at :80 (`mailto:info@american-creator.ru`
    to `mailto:info@example.com.tr`) and the display text at :92 (`info@american-creator.ru` to
    `info@example.com.tr`). Use a de-RU placeholder that does NOT contain the string `american-creator`
    (the RU-email grep gate matches `info@american-creator`).
    SOCIALS (D-04) — edit the `SOCIALS` array in Footer.tsx:8-17: DELETE the VK entry (:11,
    `soc-vk.png` / vk.com) and the Wildberries entry (:13-14, `soc-wb.svg` / wildberries.ru); REMOVE
    the Telegram entry (:9, RU handle `americancreator_ru`) entirely (planner decision per D-04: drop
    the RU handle, do not keep `_ru`); KEEP the WhatsApp entry but point its href at the TR placeholder
    number (`wa.me/905000000000`); ADD an Instagram entry `{ icon: '/icons/soc-instagram.png',
    href: 'https://www.instagram.com/', label: 'Instagram' }` (placeholder handle pending go-live).
    The array-driven render loop (:34-59) needs no structural change. Delete the orphaned assets
    `public/icons/soc-vk.png`, `public/icons/soc-wb.svg`, and `public/icons/soc-telegram.png`.
    Create `public/icons/soc-instagram.png` — generate a neutral placeholder via `~/generate_image.py`
    (per global CLAUDE.md); treat it as a placeholder like every other TR brand swap.
  </action>
  <verify>
    <automated>[ "$(git grep -n '757-84-67\|79957578467\|info@american-creator' -- src | wc -l)" = "0" ] && [ "$(git grep -ni 'vk\.com\|wildberries\|americancreator_ru' -- src | wc -l)" = "0" ] && [ -e public/icons/soc-instagram.png ] && [ ! -e public/icons/soc-vk.png ] && [ ! -e public/icons/soc-wb.svg ] && npx tsc --noEmit</automated>
    <human-check>Run `npm run dev`; load `/tr` footer and `/tr/contacts` then confirm TR phone `+90 500 000 00 00`, TR placeholder email, social icons show WhatsApp + Instagram only (no VK/WB/Telegram), all links resolve.</human-check>
  </verify>
  <acceptance_criteria>
    - `git grep -n "757-84-67\|79957578467\|info@american-creator" -- src` returns 0 (phone + email swapped at all sites)
    - `git grep -ni "vk\.com\|wildberries\|americancreator_ru" -- src` returns 0 (RU socials gone)
    - SOCIALS array contains WhatsApp (TR number) + Instagram; VK/WB/Telegram entries removed
    - `public/icons/soc-vk.png`, `soc-wb.svg`, `soc-telegram.png` absent; `soc-instagram.png` present
    - `npx tsc --noEmit` returns 0 errors
  </acceptance_criteria>
  <done>Phone (4 sites) + email swapped to TR placeholders; VK/WB/Telegram-RU dropped; WhatsApp/Instagram TR socials; assets updated; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Swap Footer payment icons to Visa/Mastercard/Troy (D-05)</name>
  <files>src/components/Footer.tsx, public/icons/paykeeper.png, public/icons/pay-systems.png, public/icons/payment.svg, public/icons/pay-troy.png</files>
  <read_first>
    - 06-CONTEXT.md then D-05 (drop yandex_money/webmoney/qiwi; show Visa/Mastercard/Troy; replace/remove paykeeper.png)
    - 06-PATTERNS.md then "Footer.tsx — payment icon rendering (D-05)" (PAYMENT_ICONS :19-25, sprite render :236-250, standalone <img> analog :235; Troy via standalone <img>)
    - 06-RESEARCH.md then "Don't Hand-Roll" (Troy: neutral flat asset, not a trademark reproduction) + State of the Art (pay-systems.png/payment.svg are verified-unused orphans)
    - src/components/Footer.tsx (PAYMENT_ICONS array + render region :230-252)
  </read_first>
  <action>
    In Footer.tsx edit the `PAYMENT_ICONS` array (:19-25): DELETE the `yandex_money` (:22),
    `webmoney` (:23), and `qiwi` (:24) entries; KEEP `mastercard` (:20) and `visa` (:21) — they render
    off the existing `payment-sprite.svg` (do NOT edit that sprite or the mastercard/visa bgPos).
    Remove the standalone PayKeeper `<img src="/icons/paykeeper.png" … />` at :235.
    ADD Troy using the existing standalone-`<img>` idiom (per PATTERNS analog), placed inside the same
    flex row (`Box sx={{ display: 'flex', gap: 0.8, alignItems: 'center' }}` at :236) alongside the
    sprite-rendered Visa/Mastercard boxes: `<img src="/icons/pay-troy.png" alt="Troy" style={{ width: 24, height: 16 }} />`.
    Do NOT introduce a new sprite position (avoids misaligning the shared sprite).
    Delete the orphaned/RU assets: `public/icons/paykeeper.png`, `public/icons/pay-systems.png`,
    `public/icons/payment.svg` (verified unused per RESEARCH). Create `public/icons/pay-troy.png` —
    generate a neutral, non-trademark "TROY" badge placeholder via `~/generate_image.py` (per global
    CLAUDE.md); treat as placeholder pending a licensed asset at go-live.
    ISOLATION GUARD: do not touch KDV display (kdvFromBrutto), KVKK/mesafeli consent, or /legal/[slug]
    (Phase 5) — none are in this file's scope.
  </action>
  <verify>
    <automated>[ "$(git grep -ni 'paykeeper\|yandex_money\|webmoney\|qiwi' -- src | wc -l)" = "0" ] && [ -e public/icons/pay-troy.png ] && [ ! -e public/icons/paykeeper.png ] && [ ! -e public/icons/pay-systems.png ] && [ ! -e public/icons/payment.svg ] && grep -q "pay-troy" src/components/Footer.tsx && npx tsc --noEmit && npx vitest run</automated>
    <human-check>Reload `/tr` footer; confirm payment row shows Visa, Mastercard, and a Troy mark — no yandex_money/webmoney/qiwi/PayKeeper icons, no broken images.</human-check>
  </verify>
  <acceptance_criteria>
    - `git grep -ni "paykeeper\|yandex_money\|webmoney\|qiwi" -- src` returns 0
    - `PAYMENT_ICONS` contains only `mastercard` + `visa`; Troy rendered via standalone `<img src="/icons/pay-troy.png">`
    - `public/icons/paykeeper.png`, `pay-systems.png`, `payment.svg` absent; `pay-troy.png` present
    - KDV/KVKK/legal untouched (not in Footer scope): `git grep -n "kdvFromBrutto\|kvkk\|/legal/" -- src` still > 0 elsewhere
    - `npx tsc --noEmit` clean; `npx vitest run` no NEW failures vs baseline
  </acceptance_criteria>
  <done>PAYMENT_ICONS shows Visa/Mastercard/Troy; RU/orphan payment assets removed; pay-troy.png added; KDV/legal untouched; tsc + vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brand-swap edit ↔ Phase-5 compliance code | Footer also hosts legal-column links; edits must not disturb KDV/KVKK/mesafeli/legal (compliance/legal integrity) |
| new placeholder social/payment href | outbound links (wa.me, instagram.com) must be static same-origin-safe placeholders, not user-templated |
| generated placeholder asset | pay-troy.png / soc-instagram.png are locally-generated placeholders, not licensed trademarks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering (compliance regression) | src/components/Footer.tsx | mitigate | Edit only SOCIALS/PAYMENT_ICONS/phone; do NOT touch KDV (kdvFromBrutto), KVKK/mesafeli consent, or /legal/[slug] (Phase 5); grep-guard those still present; tsc + vitest green |
| T-06-11 | Information Disclosure (stale RU identity) | Footer/Header/contacts | mitigate | Replace RU phone/email/socials with de-RU placeholders at ALL sites; grep gates `757-84-67`/`79957578467`/`info@american-creator`/`vk.com`/`wildberries`/`americancreator_ru` → 0 |
| T-06-SC | Tampering (supply chain) | ~/generate_image.py asset generation | accept | Not a package install — local user script per global CLAUDE.md; generated files are static placeholder images (no code execution surface); RESEARCH "Package Legitimacy Audit: N/A" |

Block-on-high: no `high`-severity threat introduced. Troy/Instagram assets are placeholders (A1/A2 assumptions), replaced with licensed assets at go-live (deploy-track).
</threat_model>

## Artifacts this phase produces

Brand-swap slice — the phase's only NEW files/symbols live here:
- NEW `public/icons/pay-troy.png` — Troy payment icon placeholder (generated).
- NEW `public/icons/soc-instagram.png` — Instagram social icon placeholder (generated).
- NEW placeholder VALUES: TR phone `+90 500 000 00 00` (`tel:+905000000000`), placeholder email `info@example.com.tr`, WhatsApp `wa.me/905000000000`, Instagram `https://www.instagram.com/`.
- EDITED `src/components/Footer.tsx`, `src/components/Header.tsx`, `src/app/[locale]/contacts/page.tsx`.
- REMOVED assets: `soc-vk.png`, `soc-wb.svg`, `soc-telegram.png`, `paykeeper.png`, `pay-systems.png`, `payment.svg`.
- No new exported code symbols (arrays/strings/assets only).

<verification>
- `git grep -n "757-84-67\|79957578467\|info@american-creator" -- src` → 0 (VALIDATION: RU phone/email gone)
- `git grep -ni "vk\.com\|wildberries\|americancreator_ru" -- src` → 0 (VALIDATION: RU socials gone)
- `git grep -ni "paykeeper\|yandex_money\|webmoney\|qiwi" -- src` → 0 (VALIDATION: PayKeeper/RU-pay gone)
- Live-guard: `git grep -n "kdvFromBrutto\|/legal/\|mesafeli\|kvkk" -- src` → still > 0 (Phase 5 untouched)
- `npx tsc --noEmit` clean; `npx vitest run` no NEW failures vs baseline
- Manual: /tr footer + /tr/contacts show TR phone/email, WhatsApp+Instagram, Visa/Mastercard/Troy (human-check)
</verification>

<success_criteria>
- CLEAN-02 satisfied: phone (D-03), email (D-10), socials (D-04), payment icons (D-05) all swapped to TR placeholders
- All RU brand grep gates return 0; Phase-5 compliance code demonstrably untouched
- pay-troy.png + soc-instagram.png placeholders created; tsc + vitest green
</success_criteria>

<output>
Create `.planning/phases/06-oms-tr/06-05-SUMMARY.md` when done.
</output>
