---
phase: 06-oms-tr
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - src/app/[locale]/delivery/page.tsx
  - public/images/delivery/payment-systems.png
  - messages/en.json
  - messages/tr.json
autonomous: true
requirements: [CLEAN-01, CLEAN-02]

must_haves:
  truths:
    - "D-02 / CLEAN-01: /delivery renders neutral TR delivery copy — no CDEK vocabulary, no CDEK_OPTIONS array, no delivery.cdek*/cityNote*/freeBanner i18n keys, no named carrier"
    - "D-06 / CLEAN-02: the RU-composite payment-systems.png image block is removed from /delivery and the asset deleted; payment methods conveyed as text (Visa/Mastercard/Troy) in reworked TR copy"
    - "/delivery still renders (page kept, not deleted per D-02); Footer 'Delivery & Payment' link still resolves"
    - "delivery.title/desc/paymentTitle/paymentDesc reworked to TR content; EN/TR parity held"
  artifacts:
    - path: "src/app/[locale]/delivery/page.tsx"
      provides: "TR delivery content page (no CDEK, no payment-systems.png), kept route"
      contains: "useTranslations"
  key_links:
    - from: "src/components/Footer.tsx"
      to: "src/app/[locale]/delivery/page.tsx"
      via: "Delivery & Payment footer link (unchanged, still resolves)"
      pattern: "/delivery"
---

<objective>
Rewrite `/delivery` for the TR market (D-02, CLEAN-01) — keep the route (Footer links it), but strip
all CDEK specifics: the `CDEK_OPTIONS` array + its render loop and the `delivery.cdek*/cityNote*/freeBanner`
i18n keys (11). Replace with neutral TR delivery copy (no named carrier — the real TR carrier is a
deploy-track item). Also remove the RU-composite payment image (D-06, CLEAN-02): drop the
`payment-systems.png` `<Box component="img">` block and delete the asset, conveying accepted methods
(Visa / Mastercard / Troy) in the reworked TR payment copy instead.

Purpose: /delivery currently describes Russian courier options (CDEK) and shows an RU payment
composite — wrong for a TR store. This slice makes it a clean, carrier-agnostic TR delivery page.
Output: A /delivery page with TR copy, no CDEK, no RU payment image.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-oms-tr/06-RESEARCH.md
@.planning/phases/06-oms-tr/06-PATTERNS.md
@.planning/phases/06-oms-tr/06-VALIDATION.md
@src/app/[locale]/delivery/page.tsx
@src/app/[locale]/faq/page.tsx
</context>

## Phase Goal

**As a** Turkish shopper, **I want to** browse a storefront with no leftover Russian-market
features (auto-promo banners, non-functional reviews, RU recruitment pages, CDEK delivery copy,
RU contacts and payment methods), **so that** the shop reads as a genuine Turkish store I can trust.

<tasks>

<task type="auto">
  <name>Task 1: Replace CDEK options with neutral TR delivery copy (+ purge cdek i18n keys)</name>
  <files>src/app/[locale]/delivery/page.tsx, messages/en.json, messages/tr.json</files>
  <read_first>
    - 06-CONTEXT.md then D-02 (rewrite, do NOT delete; neutral TR text, no carrier)
    - 06-PATTERNS.md then "delivery/page.tsx TR rework (D-02)" (faq/page.tsx analog; CDEK_OPTIONS at :11-15, render loop at :116)
    - src/app/[locale]/delivery/page.tsx (full) and src/app/[locale]/faq/page.tsx (static-content page shape)
    - messages/en.json delivery.* block (keys to remove vs keys to rework)
  </read_first>
  <action>
    In src/app/[locale]/delivery/page.tsx delete the `CDEK_OPTIONS` array (:11-15, the three
    `{ name: t('cdek0Name'), time: t('cdek0Time') }` entries) and its render loop
    (`{CDEK_OPTIONS.map(...)}` at :116 and the surrounding bordered `<Box>` cards). Replace with a
    neutral TR delivery-copy block reusing the SAME static-page shape already present (keep breadcrumb
    + h1 + `useTranslations('delivery')` — structurally identical to faq/page.tsx). Do NOT name a
    specific carrier (deploy-track). Keep the `delivery.title` and `delivery.desc` keys but rework
    their VALUES to neutral TR/EN delivery text (no CDEK, no RU courier framing).
    In messages/en.json AND messages/tr.json remove these 11 keys from EACH file:
    `delivery.cdek0Name`, `delivery.cdek0Time`, `delivery.cdek1Name`, `delivery.cdek1Time`,
    `delivery.cdek2Name`, `delivery.cdek2Time`, `delivery.cityNote1`, `delivery.cityNote2Line1`,
    `delivery.cityNote2Line2`, `delivery.cityNote2Line3`, `delivery.freeBanner`. Rework the VALUES of
    `delivery.title`/`delivery.desc` (keep both keys present). Write real Turkish strings in tr.json
    and English in en.json. Maintain EN/TR parity (after this task count drops from 295 to 284; Task 2
    removes 1 more, final plan count = 283).
  </action>
  <verify>
    <automated>[ "$(git grep -ni 'cdek' -- src | wc -l)" = "0" ] && python3 -c "import json; en=json.load(open('messages/en.json')); tr=json.load(open('messages/tr.json')); assert set(en)==set(tr), set(en)^set(tr); assert not any(k.startswith('delivery.cdek') or k.startswith('delivery.cityNote') or k=='delivery.freeBanner' for k in en); assert 'delivery.title' in en and 'delivery.desc' in en; print('parity',len(en))" && npx tsc --noEmit</automated>
    <human-check>Run `npm run dev`; load `/tr/delivery` and `/en/delivery` then confirm TR/EN delivery copy renders, no CDEK wording, no dotted-key strings; Footer "Delivery and Payment" link reaches it.</human-check>
  </verify>
  <acceptance_criteria>
    - `git grep -ni "cdek" -- src` returns 0
    - No `delivery.cdek*`/`delivery.cityNote*`/`delivery.freeBanner` key remains; `delivery.title`/`delivery.desc` present with reworked values
    - `set(en)==set(tr)` (parity held)
    - `npx tsc --noEmit` returns 0 errors; /delivery still renders (route kept)
  </acceptance_criteria>
  <done>CDEK options + keys removed; /delivery shows neutral TR copy; route kept; parity + tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the RU payment-systems.png image block + asset (D-06)</name>
  <files>src/app/[locale]/delivery/page.tsx, public/images/delivery/payment-systems.png, messages/en.json, messages/tr.json</files>
  <read_first>
    - 06-CONTEXT.md then D-06 (replace with TR set OR remove the image block; planner chose remove)
    - 06-PATTERNS.md then delivery rework point 2 (payment image `<Box component="img">` at :263-265, `paymentImgAlt`)
    - src/app/[locale]/delivery/page.tsx (payment section: paymentTitle/paymentDesc + image block at :263-265)
  </read_first>
  <action>
    In src/app/[locale]/delivery/page.tsx remove the payment image block
    `<Box component="img" src="/images/delivery/payment-systems.png" alt={t('paymentImgAlt')} … />`
    (around :263-267). Keep the payment text section (`delivery.paymentTitle`/`delivery.paymentDesc`)
    but rework the `delivery.paymentDesc` VALUE to name the TR-accepted methods in prose: Visa,
    Mastercard, and Troy (drop RU "МИР"/RU-specific wording). Delete the now-unused
    `delivery.paymentImgAlt` key from BOTH messages files (its only consumer was the removed image
    block). Delete the asset `public/images/delivery/payment-systems.png`. Maintain EN/TR parity
    (final plan count = 283 keys each: 284 − 1).
  </action>
  <verify>
    <automated>[ ! -e "public/images/delivery/payment-systems.png" ] && [ "$(git grep -n 'payment-systems\|paymentImgAlt' -- src | wc -l)" = "0" ] && python3 -c "import json; en=json.load(open('messages/en.json')); tr=json.load(open('messages/tr.json')); assert set(en)==set(tr), set(en)^set(tr); assert 'delivery.paymentImgAlt' not in en; assert 'delivery.paymentDesc' in en; print('parity',len(en))" && npx tsc --noEmit && npx vitest run</automated>
    <human-check>Reload `/tr/delivery`; confirm no broken payment image, payment copy mentions Visa/Mastercard/Troy in TR.</human-check>
  </verify>
  <acceptance_criteria>
    - `public/images/delivery/payment-systems.png` absent; `git grep -n "payment-systems\|paymentImgAlt" -- src` returns 0
    - `delivery.paymentImgAlt` removed both locales; `delivery.paymentDesc` present with Visa/Mastercard/Troy TR text
    - `set(en)==set(tr)`; 283 keys each
    - `npx tsc --noEmit` clean; `npx vitest run` no NEW failures vs baseline
  </acceptance_criteria>
  <done>payment-systems.png block + asset removed; payment methods conveyed as TR text; parity + tsc + vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| removed i18n key → surviving /delivery render | a `t('cdek0Name')`/`t('paymentImgAlt')` call after key removal renders the literal dotted string (silent defect, availability) |
| deleted asset → page image reference | a removed image asset still referenced by `src=` yields a broken image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Denial of Service (render) | delivery/page.tsx + messages/{en,tr}.json | mitigate | Remove `CDEK_OPTIONS` render loop and `paymentImgAlt` usage IN THE SAME task as the key/asset removals; grep `cdek`/`payment-systems`/`paymentImgAlt` in src → 0; tsc/build green; human-check confirms no dotted-key strings render |
| T-06-09 | Information Disclosure (stale RU content) | delivery/page.tsx copy | mitigate | Rework `delivery.desc`/`delivery.paymentDesc` values so no RU courier / RU payment wording survives; grep `cdek` → 0 |
| T-06-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH audit N/A) |

Block-on-high: no `high`-severity threat introduced.
</threat_model>

## Artifacts this phase produces

Rework + deletion slice:
- EDITED `src/app/[locale]/delivery/page.tsx` — TR delivery content, no CDEK_OPTIONS, no payment image block.
- REWORKED VALUES (same keys): `delivery.title`, `delivery.desc`, `delivery.paymentTitle`, `delivery.paymentDesc` — new TR/EN strings (Visa/Mastercard/Troy in payment copy).
- REMOVED: 11 `delivery.cdek*`/`cityNote*`/`freeBanner` keys + `delivery.paymentImgAlt` (both locales), `public/images/delivery/payment-systems.png`.
- No new files or exported symbols created by this plan.

<verification>
- `git grep -ni "cdek" -- src` → 0 (VALIDATION: CDEK gone)
- `git grep -n "payment-systems\|paymentImgAlt" -- src` → 0; asset deleted (D-06)
- i18n parity script green (283/283)
- `npx tsc --noEmit` clean; `npx vitest run` no NEW failures vs baseline
- Manual: /tr/delivery renders TR copy, no CDEK, no broken image (human-check)
</verification>

<success_criteria>
- CLEAN-01 (CDEK portion) satisfied: no CDEK code/keys; /delivery kept and rendering
- CLEAN-02 (D-06) satisfied: RU payment image + asset removed; TR payment methods in copy
- D-02 + D-06 honored exactly (route kept, no named carrier); EN/TR parity held; tsc + vitest green
</success_criteria>

<output>
Create `.planning/phases/06-oms-tr/06-04-SUMMARY.md` when done.
</output>
