---
phase: 06-oms-tr
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - src/app/[locale]/partners/
  - src/app/[locale]/studios/
  - src/components/Header.tsx
  - src/components/Footer.tsx
  - src/app/sitemap.ts
  - next.config.js
  - public/images/partners/
  - public/images/studios/
  - messages/en.json
  - messages/tr.json
autonomous: true
requirements: [CLEAN-01]

must_haves:
  truths:
    - "D-01 / CLEAN-01: /partners (+ /partners/shops,/bloggers,/schools) and /studios routes, their nav links in Header.tsx and Footer.tsx, their sitemap STATIC_PATHS entries, and their public images are removed — the routes 404"
    - "D-01: studios.*, partners.*, nav.studios, nav.partners i18n keys (77) removed in both locales with EN/TR parity"
    - "D-07 / CLEAN-01: next.config.js redirects() contains ONLY trailing-slash hygiene for existing TR routes (/contacts/, /basket/, /catalog/); all RU/Bitrix redirects (categoryMap, .php, /ankety*, /auth*, /personal*, /help/delivery, /info/faq, /novinki.php) are gone; every surviving redirect targets an existing route"
    - "sitemap.xml no longer advertises dead /partners*//studios URLs (RESEARCH gap #1)"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "STATIC_PATHS without partners/studios entries"
      contains: "STATIC_PATHS"
    - path: "next.config.js"
      provides: "redirects() pruned to trailing-slash hygiene only"
      contains: "redirects"
    - path: "src/components/Header.tsx"
      provides: "primary nav without studios/partners links (nav.new and other items retained)"
      contains: "nav.new"
  key_links:
    - from: "src/components/Header.tsx"
      to: "removed routes"
      via: "nav.studios/nav.partners links deleted (lines 60-61)"
      pattern: "nav.studios|nav.partners"
    - from: "src/app/sitemap.ts"
      to: "removed routes"
      via: "STATIC_PATHS partners/studios entries deleted (lines 28-32)"
      pattern: "/partners|/studios"
---

<objective>
Remove the RU-recruitment business pages (D-01, CLEAN-01): the `/partners` route + its three
subpages (`/partners/shops`, `/partners/bloggers`, `/partners/schools`) and `/studios`, together
with their nav links (Header + Footer), their `sitemap.ts` STATIC_PATHS entries (RESEARCH gap #1 —
otherwise sitemap.xml advertises 404 URLs), their public images, and their 77 i18n keys. Then prune
`next.config.js` redirects (D-07, CLEAN-01): delete all legacy Bitrix/RU redirects, keeping only
trailing-slash hygiene for existing TR routes.

Purpose: These pages recruit RU nail-studios/bloggers/schools — meaningless in the TR model. This
slice removes an entire RU marketing surface + the legacy Bitrix URL-migration machinery a TR-native
site never needs.
Output: `/partners*` and `/studios` 404; nav/sitemap/redirects carry no dead RU URLs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-oms-tr/06-RESEARCH.md
@.planning/phases/06-oms-tr/06-PATTERNS.md
@.planning/phases/06-oms-tr/06-VALIDATION.md
@src/app/sitemap.ts
@next.config.js
</context>

## Phase Goal

**As a** Turkish shopper, **I want to** browse a storefront with no leftover Russian-market
features (auto-promo banners, non-functional reviews, RU recruitment pages, CDEK delivery copy,
RU contacts and payment methods), **so that** the shop reads as a genuine Turkish store I can trust.

<tasks>

<task type="auto">
  <name>Task 1: Delete partners/studios routes, nav links, sitemap entries, and images</name>
  <files>src/app/[locale]/partners/, src/app/[locale]/studios/, src/components/Header.tsx, src/components/Footer.tsx, src/app/sitemap.ts, public/images/partners/, public/images/studios/</files>
  <read_first>
    - 06-CONTEXT.md → D-01 (full removal list incl. RESEARCH addendum on sitemap.ts)
    - 06-RESEARCH.md → Summary point 1 (sitemap STATIC_PATHS gap) + "Recommended Task Structure"
    - src/app/sitemap.ts (STATIC_PATHS array, lines 28-32)
    - src/components/Header.tsx (nav array lines 59-61) and src/components/Footer.tsx (nav lines 67-68)
  </read_first>
  <action>
    Delete the entire directories `src/app/[locale]/partners/` (page.tsx + bloggers/schools/shops
    subpages) and `src/app/[locale]/studios/`. Delete their image assets: `public/images/partners/`
    and `public/images/studios/` (verified used only by those pages).
    In src/components/Header.tsx remove the two nav entries at :60-61 (`{ label: t('nav.studios'), href: '/studios' }`
    and `{ label: t('nav.partners'), href: '/partners' }`). KEEP the `nav.new` entry at :59
    (`href: '/catalog?sort=-date_created'`) and all other nav items.
    In src/components/Footer.tsx remove the two nav entries at :67-68 (`{ label: t('nav.studios'), href: '/studios' }`
    and `{ label: t('nav.partners'), href: '/partners' }`). Keep the "Delivery & Payment" footer link
    (D-02 keeps /delivery).
    In src/app/sitemap.ts remove the five STATIC_PATHS entries at :28-32: `'/partners'`,
    `'/partners/shops'`, `'/partners/bloggers'`, `'/partners/schools'`, `'/studios'`. Keep all other
    STATIC_PATHS (e.g. /catalog, /delivery, /faq, /contacts).
  </action>
  <verify>
    <automated>[ "$(git grep -n "'/studios'\|'/partners" -- src | wc -l)" = "0" ] && [ ! -e "src/app/[locale]/partners" ] && [ ! -e "src/app/[locale]/studios" ] && npx tsc --noEmit && npx vitest run src/app/sitemap.test.ts</automated>
    <human-check>Run `npm run dev` (ACTR on :3003); GET `/tr/partners`, `/tr/partners/shops`, `/tr/studios`, `/en/partners`, `/en/studios` → all 404. Confirm the header/footer no longer show Studios/Partners links.</human-check>
  </verify>
  <acceptance_criteria>
    - `git grep -n "'/studios'\|'/partners" -- src` → 0 (routes, nav, sitemap all gone)
    - `src/app/[locale]/partners`, `src/app/[locale]/studios`, `public/images/partners`, `public/images/studios` → absent
    - `git grep -n "nav.studios\|nav.partners" -- src/components` → 0
    - `npx vitest run src/app/sitemap.test.ts` → green (no partners/studios in output)
    - `npx tsc --noEmit` → 0 errors
  </acceptance_criteria>
  <done>partners/studios routes, nav links, sitemap entries, and images removed; routes 404; tsc + sitemap test green.</done>
</task>

<task type="auto">
  <name>Task 2: Purge studios.*/partners.*/nav.* i18n keys (EN/TR parity)</name>
  <files>messages/en.json, messages/tr.json</files>
  <read_first>
    - 06-VALIDATION.md → "i18n Parity Gate" (studios./partners./nav.studios/nav.partners)
    - 06-PATTERNS.md → "i18n key removal — flat-dotted parity"
    - messages/en.json (key prefixes studios., partners., and nav.studios/nav.partners)
  </read_first>
  <action>
    In messages/en.json AND messages/tr.json remove from EACH file every key with prefix `studios.`
    (28 keys), every key with prefix `partners.` (47 keys, incl. `partners.shops.*`/`partners.bloggers.*`/
    `partners.schools.*`/`partners.quick*`), plus the two keys `nav.studios` and `nav.partners`
    (77 keys total per locale). KEEP `nav.new` and every other `nav.*` key. Maintain EN/TR parity
    (295 keys each after this plan: 372 from Plan 02 − 77).
  </action>
  <verify>
    <automated>python3 -c "import json; en=json.load(open('messages/en.json')); tr=json.load(open('messages/tr.json')); bad=[k for k in en if k.startswith('studios.') or k.startswith('partners.') or k in ('nav.studios','nav.partners')]; assert set(en)==set(tr), set(en)^set(tr); assert not bad, bad; print('parity',len(en))" && [ "$(git grep -n "'studios\.\|'partners\.\|nav.studios\|nav.partners" -- src | wc -l)" = "0" ] && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - No `studios.*`, `partners.*`, `nav.studios`, or `nav.partners` key remains in either messages file
    - `set(en)==set(tr)`; 295 keys each
    - No surviving src references a removed key prefix: `git grep -n "studios\.\|partners\.\|nav.studios\|nav.partners" -- src` → 0 (excludes deleted files)
    - `npx tsc --noEmit` → 0 errors
  </acceptance_criteria>
  <done>77 partners/studios/nav keys purged in both locales with parity held; no dangling references; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Prune next.config.js redirects to trailing-slash hygiene only (D-07)</name>
  <files>next.config.js</files>
  <read_first>
    - 06-CONTEXT.md → D-07 (keep list vs remove list) + 06-RESEARCH.md "Anti-Patterns to Avoid" (verify remaining destinations exist)
    - next.config.js (full redirects() function, lines 11-149)
    - 06-VALIDATION.md → "Bitrix redirects gone" gate
  </read_first>
  <action>
    Rewrite `redirects()` in next.config.js to KEEP only trailing-slash hygiene for existing TR routes:
    `/contacts/` → `/contacts` (:35), `/basket/` → `/basket` (:38), and `/catalog/` → `/catalog` (:147).
    REMOVE everything else: the `categoryMap` object and both `for … of Object.entries(categoryMap)`
    loops (:13-20, :109-120, :133-144); `/help/delivery` + `/help/delivery/` (:27-28); `/info/faq` +
    `/info/faq/` (:31-32); `/novinki.php` (:41-45); all `/ankety*` incl. the `.php` variants (:48-65);
    all `/auth*` (:68-71); all `/personal*` (:74-76); `/catalog/compare.php` (:79); the `vse_tovary`
    and `must_have` product+category redirects (:85-106, :124-130). Keep `output: 'standalone'`,
    `skipTrailingSlashRedirect: true`, and the Sentry/next-intl wrapping unchanged.
    Confirm the three surviving redirect destinations (`/contacts`, `/basket`, `/catalog`) are existing
    TR routes (they are). Do NOT introduce any redirect whose destination is templated from user input
    or query params (open-redirect guard, ASVS V14) — all three destinations are static same-origin
    literals.
  </action>
  <verify>
    <automated>[ "$(grep -cE "\.php|ankety|/personal|/auth\b|categoryMap|vse_tovary|must_have|novinki|help/delivery|info/faq" next.config.js)" = "0" ] && node -e "require('./next.config.js')" && npx tsc --noEmit</automated>
    <human-check>Run `npm run dev`; `curl -sI http://localhost:3003/basket/` and `/contacts/` → 301 to the slash-less route; `curl -sI http://localhost:3003/help/delivery` and `/ankety` → 404 (removed).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\.php|ankety|/personal|/auth\b|categoryMap|vse_tovary|must_have|novinki|help/delivery|info/faq" next.config.js` → 0
    - `redirects()` contains only the three trailing-slash hygiene entries; all destinations are existing TR routes
    - `node -e "require('./next.config.js')"` loads without error (syntax valid)
    - No destination is derived from user input (open-redirect guard holds)
  </acceptance_criteria>
  <done>next.config.js redirects pruned to trailing-slash hygiene only; no RU/Bitrix redirects; config loads; open-redirect invariant confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| incoming URL → next.config.js redirects() | edge route resolution before any page renders; a redirect `destination` templated from user input would be an open-redirect |
| deleted route → nav/sitemap references | a nav link or sitemap entry pointing at a deleted route yields a dead link / 404 URL advertised to crawlers |
| removed i18n key → surviving pages | a page still calling `t('partners.…')` renders the literal dotted string (silent defect, availability) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering (open redirect) | next.config.js redirects() | mitigate | D-07 edit only REMOVES entries; the three surviving destinations (`/contacts`,`/basket`,`/catalog`) are static same-origin literals, never templated from query/user input (ASVS V14); `node -e require` confirms valid syntax |
| T-06-07 | Denial of Service (dead links / bad sitemap) | Header.tsx, Footer.tsx, sitemap.ts | mitigate | Remove nav links AND sitemap STATIC_PATHS in the same task as the routes; grep `'/partners|'/studios` → 0 across src; sitemap.test.ts green |
| T-06-05 | Denial of Service (render) | messages/{en,tr}.json | mitigate | After removing 77 keys, grep surviving src for `studios.`/`partners.`/`nav.studios`/`nav.partners` (expect 0); tsc/build green |
| T-06-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH audit N/A) |

Block-on-high: no `high`-severity threat. Open-redirect (V14) explicitly confirmed non-applicable after the edit (removal-only).
</threat_model>

## Artifacts this phase produces

Deletion slice — surviving/edited artifacts (no new exported symbols):
- EDITED `src/app/sitemap.ts` — STATIC_PATHS without partners/studios.
- EDITED `next.config.js` — `redirects()` reduced to 3 trailing-slash hygiene entries.
- EDITED `src/components/Header.tsx`, `src/components/Footer.tsx` — nav arrays without studios/partners links.
- REMOVED: `src/app/[locale]/partners/**`, `src/app/[locale]/studios/**`, `public/images/{partners,studios}/**`, 77 `studios.*`/`partners.*`/`nav.*` keys (both locales).
- No new files or exported symbols created by this plan.

<verification>
- `git grep -n "'/studios'\|'/partners\|nav.studios\|nav.partners" -- src` → 0 (VALIDATION: RU pages gone)
- `grep -cE "\.php|ankety|/personal|/auth\b|categoryMap" next.config.js` → 0 (VALIDATION: Bitrix redirects gone)
- i18n parity script green (295/295, no studios./partners./nav.studios/nav.partners)
- `npx tsc --noEmit` clean; `npx vitest run` no NEW failures vs baseline; sitemap.test.ts green
- Manual: `/tr/partners`,`/tr/studios` → 404; kept redirects resolve (human-check above)
</verification>

<success_criteria>
- CLEAN-01 (RU pages + redirects) satisfied: routes, nav, sitemap, images, i18n keys removed; redirects pruned
- D-01 + D-07 honored exactly (incl. RESEARCH sitemap gap #1); surviving redirects target existing routes
- EN/TR parity preserved; tsc clean; no new vitest failures; open-redirect invariant confirmed
</success_criteria>

<output>
Create `.planning/phases/06-oms-tr/06-03-SUMMARY.md` when done.
</output>
