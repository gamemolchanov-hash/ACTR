---
phase: 06-oms-tr
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/en.json
  - messages/tr.json
  - src/components/Footer.tsx
autonomous: true
gap_closure: true
requirements: [CLEAN-01, CLEAN-02]

must_haves:
  truths:
    - "CLEAN-01 gap-1 (blocker) now passing: no Russian-market legal/seller-identity remains on the TR storefront contacts page — contacts.legalLine1-5 in BOTH messages/en.json and messages/tr.json render only a TR seller-identity disclosure using the literal [Placeholder] token; no RU registered address, RU-bank name, or RUB current/correspondent account in either locale."
    - "CLEAN-02 gap-2 (warning) now passing: no residual RU-project domain in the live site-wide footer copyright — Footer.tsx:214 reads the neutral brand line with a dynamic year via new Date().getFullYear(); the domain is dropped entirely."
    - "EN/TR i18n parity preserved: value-only swap of 5 existing keys — no key added or removed; both files parse as JSON, identical key sets, 283/283 keys each."
    - "No previously-verified TR swap regressed: phone/email/socials/payment icons (VERIFICATION truths #10-13) and nav remain TR; only the two named residue surfaces change."
  prohibitions:
    - "MUST NOT ship a fabricated real-looking TR or RU legal address, VKN/MERSİS number, or IBAN — every not-yet-known seller-identity value uses the literal [Placeholder] token (matches the Phase-5 legal-page precedent legal.kvkk.* / legal.mesafeli_satis.*)."
  artifacts:
    - path: "messages/en.json"
      provides: "contacts.legalLine1-5 EN values swapped to the TR seller-identity disclosure (labels in English, [Placeholder] token for pending facts)"
      contains: "[Placeholder]"
    - path: "messages/tr.json"
      provides: "contacts.legalLine1-5 TR values swapped to the TR seller-identity disclosure (labels in Turkish, [Placeholder] token for pending facts)"
      contains: "[Placeholder]"
    - path: "src/components/Footer.tsx"
      provides: "Site-wide footer copyright as a neutral, deploy-agnostic brand line with a dynamic year"
      contains: "getFullYear"
  key_links:
    - from: "src/app/[locale]/contacts/page.tsx"
      to: "messages/{en,tr}.json contacts.legalLine1-5"
      via: "t('legalLine1')..t('legalLine5') already wired at page.tsx:309-317 (5 keys + 4 <br/>) — no JSX edit"
      pattern: "legalLine"
    - from: "src/components/Footer.tsx:214"
      to: "new Date().getFullYear()"
      via: "inline dynamic year in the copyright Typography text node"
      pattern: "getFullYear"
---

<objective>
Close the two residual gaps found in 06-VERIFICATION.md — and nothing else. Phase 6's five plans
(06-01..06-05) are DONE and VERIFIED; this plan does not re-touch any of that mechanical-removal work.

- Gap 1 (BLOCKER, CLEAN-01 / CLEAN-02 residue): the contacts page's "Legal Info / Satıcı Bilgileri"
  block still renders a Russian seller legal entity (a Moscow registered address + a PAO-bank RUB
  account) via contacts.legalLine1-5 in BOTH locale files. Swap all five VALUES (both locales) to a
  fixed TR seller-identity disclosure using the established [Placeholder] token — legally material for
  TR mesafeli satış compliance (hard project constraint).
- Gap 2 (WARNING, CLEAN-02 residue): Footer.tsx:214 hardcodes the RU project's own domain as the
  site-wide copyright holder. Replace it with a neutral, deploy-agnostic brand line with a dynamic
  year.

Both are content-only edits (5 i18n value swaps + 1 JSX text node). i18n parity (283/283) is held by
swapping values in place — no key add/remove. All exact strings are copied verbatim from the BINDING
06-UI-SPEC.md "Copywriting Contract" (do not re-derive).

Purpose: bring the phase's own restated goal — "the shop reads as a genuine Turkish store I can
trust" — fully true by eliminating the last RU legal-identity and RU-domain residue.
Output: TR-neutral contacts legal block ([Placeholder] convention) + neutral dynamic-year footer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-oms-tr/06-VERIFICATION.md
@.planning/phases/06-oms-tr/06-UI-SPEC.md
@.planning/phases/06-oms-tr/deferred-items.md
@messages/en.json
@messages/tr.json
@src/components/Footer.tsx
</context>

## Phase Goal

**As a** Turkish shopper, **I want to** browse a storefront with no leftover Russian-market
identity (RU seller legal address, RU bank account, the RU project's own domain in the footer),
**so that** the shop reads as a genuine Turkish store I can trust.

> Scope note (MVP): both fixes are user-visible text on `/contacts` (site of Gap 1) and the
> site-wide footer (Gap 2) — a single thin, already-vertical slice. Two disjoint-file tasks; do not
> over-decompose or add horizontal layers.

## Artifacts this phase produces

This gap closure introduces **no new files, exports, symbols, routes, or i18n keys**. It is:
- VALUE edits to 5 existing i18n keys (`contacts.legalLine1`..`legalLine5`) in each of
  `messages/en.json` and `messages/tr.json` — key set is unchanged (283/283, sets stay equal).
- One JSX **text-node** edit inside the existing copyright `<Typography>` in `src/components/Footer.tsx`
  (wrapper, `sx`, position all unchanged).
- No new component, no new prop, no new `t()` key, no new asset, no new export.
- `src/app/[locale]/contacts/page.tsx` is **read-first only** — the 5 keys + 4 `<br/>` are already
  wired (lines 309-317); no JSX/`sx` edit there is required or permitted.

<tasks>

<task type="auto">
  <name>Task 1: Swap contacts legal/seller-identity block to TR [Placeholder] disclosure (Gap 1, blocker)</name>
  <files>messages/en.json, messages/tr.json</files>
  <read_first>
    - 06-UI-SPEC.md → "Copywriting Contract → Gap 1" (BINDING value table + the mandatory
      [Placeholder] pending-field convention; copy values verbatim, do not re-derive)
    - 06-VERIFICATION.md → Gap 1 (the failed truth + the exact RU strings currently shipping)
    - messages/en.json → the 5 keys contacts.legalLine1..legalLine5 (~L220-224) so the executor sees
      the real RU seller-identity strings being replaced
    - messages/tr.json → the same 5 keys (~L220-224), TR locale (RU entity translated to Turkish)
    - messages/en.json → legal.kvkk.* / legal.mesafeli_satis.* (~L252-266): the established
      [Placeholder]-token precedent this swap must match
    - src/app/[locale]/contacts/page.tsx:298-319 (READ ONLY): confirms the 5 keys + 4 <br/> are
      already wired in the "Right legal block"; NO JSX/sx edit is needed or permitted here
  </read_first>
  <action>
    Replace the VALUES of `contacts.legalLine1` through `contacts.legalLine5` in BOTH `messages/en.json`
    and `messages/tr.json` (currently a Russian seller-identity block: a RU registered address across
    two lines + a RU-bank name and RU-bank current/correspondent account numbers) with the fixed
    5-line TR seller-identity disclosure from 06-UI-SPEC.md §Gap 1 — one fact per line. Set exactly
    these values (labels ARE translated per locale; the `[Placeholder]` token stays byte-identical in
    both files):

    | Key | messages/en.json value | messages/tr.json value |
    |-----|------------------------|------------------------|
    | contacts.legalLine1 | `Legal name: [Placeholder]` | `Ünvan: [Placeholder]` |
    | contacts.legalLine2 | `Registered address: [Placeholder]` | `Yasal adres: [Placeholder]` |
    | contacts.legalLine3 | `Tax ID / MERSİS No: [Placeholder]` | `VKN / MERSİS No: [Placeholder]` |
    | contacts.legalLine4 | `IBAN: [Placeholder]` | `IBAN: [Placeholder]` |
    | contacts.legalLine5 | `Bank: [Placeholder]` | `Banka: [Placeholder]` |

    Preserve the exact Turkish glyphs shown (`Ünvan`, `MERSİS` with the dotted capital İ). Edit VALUES
    only — do NOT add, remove, reorder, or rename any key; do NOT touch the 4 `<br/>` structure in
    page.tsx; do NOT alter the JSON formatting (2-space indent, key order, trailing commas). Per the
    UI-SPEC pending-field convention, use the literal `[Placeholder]` token for every not-yet-known
    seller-identity fact — this is MANDATORY. Never substitute a real or real-looking TR/RU address,
    VKN/MERSİS number, IBAN, or bank name (a fabricated value is a second legally-material inaccuracy;
    an honest `[Placeholder]` is not, and it is auto-surfaced by the pre-go-live `[Placeholder]` sweep).
    ISOLATION GUARD: do not touch any other key in either file (KDV/KVKK/mesafeli/legal.* from Phase 5,
    or the already-verified phone/email keys stay exactly as-is).
  </action>
  <verify>
    <automated>[ "$(git grep -n 'Moscow\|Moskova\|PAO Sberbank\|Sosenkoye\|Kommunarka\|40802810638000019658\|30101810400000000225' -- messages/ | wc -l)" = "0" ] && grep -qF '"contacts.legalLine1": "Legal name: [Placeholder]"' messages/en.json && grep -qF '"contacts.legalLine3": "Tax ID / MERSİS No: [Placeholder]"' messages/en.json && grep -qF '"contacts.legalLine5": "Bank: [Placeholder]"' messages/en.json && grep -qF '"contacts.legalLine1": "Ünvan: [Placeholder]"' messages/tr.json && grep -qF '"contacts.legalLine3": "VKN / MERSİS No: [Placeholder]"' messages/tr.json && grep -qF '"contacts.legalLine5": "Banka: [Placeholder]"' messages/tr.json && python3 -c "import json;e=json.load(open('messages/en.json'));t=json.load(open('messages/tr.json'));assert len(e)==283 and len(t)==283 and set(e)==set(t)" && npx tsc --noEmit</automated>
    <human-check>Run `npm run dev`; load `/en/contacts` and `/tr/contacts`; confirm the right-hand legal block reads the 5-line TR seller-identity disclosure with `[Placeholder]` values (no Moscow address, no PAO-bank/RUB account), correctly localized labels (EN: "Legal name…" / TR: "Ünvan…"), same fine-print styling.</human-check>
  </verify>
  <acceptance_criteria>
    - `git grep -n "Moscow\|Moskova\|PAO Sberbank\|Sosenkoye\|Kommunarka\|40802810638000019658\|30101810400000000225" -- messages/` → 0 matches (zero RU legal/bank residue in either locale)
    - `messages/en.json` `contacts.legalLine1` == `Legal name: [Placeholder]`; `legalLine3` == `Tax ID / MERSİS No: [Placeholder]`; `legalLine5` == `Bank: [Placeholder]`
    - `messages/tr.json` `contacts.legalLine1` == `Ünvan: [Placeholder]`; `legalLine3` == `VKN / MERSİS No: [Placeholder]`; `legalLine5` == `Banka: [Placeholder]`
    - Every one of the 10 swapped values contains the literal `[Placeholder]`; none contains a fabricated real-looking address/VKN/IBAN/bank
    - EN/TR parity held: both files parse as JSON, identical key sets, exactly 283 keys each (no key add/remove)
    - `npx tsc --noEmit` → 0 errors
  </acceptance_criteria>
  <done>All 5 legal lines in both locales are the TR [Placeholder] seller-identity disclosure; zero RU legal/bank residue; 283/283 parity held; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Neutralize footer copyright to dynamic-year brand line (Gap 2, warning)</name>
  <files>src/components/Footer.tsx</files>
  <read_first>
    - 06-UI-SPEC.md → "Copywriting Contract → Gap 2" (BINDING: `© {year} American Creator`, drop the
      domain, dynamic year DETERMINED, static JSX string not a t() key, keep `&copy;` entity, keep the
      existing Typography wrapper/position)
    - 06-VERIFICATION.md → Gap 2 (the partial truth: the RU-project domain shown site-wide)
    - deferred-items.md → why 06-05 logged this line as out-of-scope-then (now in-scope here)
    - src/components/Footer.tsx:205-224 → the copyright `<Typography sx={{ fontSize: 16, color:
      palette.primaryLight }}>` at :213 and its text node at :214; and the existing
      `alt="American Creator"` proper-noun precedent (~:101,:169) this static string matches
  </read_first>
  <action>
    In `src/components/Footer.tsx`, replace ONLY the copyright text node on line 214 (currently a
    hardcoded year followed by the RU project's own domain) with the neutral brand line per
    06-UI-SPEC.md §Gap 2. The new text node is exactly: `{new Date().getFullYear()} &copy; American Creator`.
    Requirements: (a) drop the domain entirely — brand name only, no URL, no substitute `.tr` domain;
    (b) compute the year dynamically inline with `new Date().getFullYear()` — DETERMINED, not a choice;
    it is SSR/hydration-safe (stable within a render) and prevents stale-year recurrence; do NOT
    hardcode a year; (c) keep it a static JSX string — do NOT add a new `t()` i18n key ("American
    Creator" is a proper noun, same EN/TR, matching the existing `alt="American Creator"` precedent in
    this same file); (d) keep the `&copy;` HTML entity (do not switch to the literal `©` glyph);
    (e) leave the wrapping `<Typography sx={{ fontSize: 16, color: palette.primaryLight }}>`, its
    styling, and its bottom-left position unchanged — no restyle, recolor, resize, or reposition.
    ISOLATION GUARD: touch only line 214; do not modify the footer nav columns, SOCIALS, PAYMENT_ICONS,
    phone, or any Phase-5 legal-column links (all already verified TR).
  </action>
  <verify>
    <automated>[ "$(git grep -n 'american-creator.ru' -- src/components/Footer.tsx | wc -l)" = "0" ] && grep -qF '{new Date().getFullYear()} &copy; American Creator' src/components/Footer.tsx && npx tsc --noEmit</automated>
    <human-check>Reload any page footer (e.g. `/tr`); confirm the bottom-left copyright reads "© {current year} American Creator" (current calendar year, no domain), same size/color/position as before; the payment icons row opposite it is unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    - `git grep -n "american-creator.ru" -- src/components/Footer.tsx` → 0 matches (RU-project domain gone from the copyright line)
    - `src/components/Footer.tsx` contains the exact text node `{new Date().getFullYear()} &copy; American Creator` (dynamic year + `&copy;` entity + brand name, no URL)
    - No new `t()` / i18n key added for the copyright (still a static JSX string; the `alt="American Creator"` precedent is unchanged)
    - The copyright `<Typography sx={{ fontSize: 16, color: palette.primaryLight }}>` wrapper, styling, and position are byte-unchanged apart from the text node
    - `npx tsc --noEmit` → 0 errors
  </acceptance_criteria>
  <done>Footer copyright is a dynamic-year neutral brand line with no domain; wrapper/style/position unchanged; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published seller-identity ↔ TR mesafeli satış compliance | The contacts legal block is the merchant-of-record disclosure a TR buyer relies on; shipping wrong OR fabricated data is legally material (hard project constraint). |
| content edit ↔ Phase-5 compliance keys | Both target files sit beside KDV/KVKK/mesafeli/legal.* keys and links; the value swap must not disturb them. |
| i18n value swap ↔ EN/TR key-set parity | A value-only edit must not add/remove keys, or it breaks the 283/283 parity guarantee downstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-12 | Information Disclosure / Tampering (fabricated seller identity) | messages/{en,tr}.json contacts.legalLine1-5 | medium | mitigate | Use the literal `[Placeholder]` token for every not-yet-known seller-identity value (Phase-5 legal-page precedent); NEVER fabricate a real-looking TR/RU address, VKN/MERSİS, IBAN, or bank. Positive grep asserts `[Placeholder]` present; residue grep asserts 0 RU legal/bank strings. Below ASVS L1 `high` block threshold → informational, but stated (per phase security_enforcement). |
| T-06-13 | Information Disclosure (stale RU-project domain in live UI) | src/components/Footer.tsx:214 | low | mitigate | Replace the RU-project domain copyright with a dynamic-year neutral brand line; negative grep on the domain in Footer.tsx → 0; `new Date().getFullYear()` removes the stale-hardcoded-value recurrence class. |
| T-06-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | N/A — this plan performs NO package-manager installs (pure text/JSX edits, no new deps or generated assets); RESEARCH Package Legitimacy Audit N/A. |

Block-on-high: no `high`-severity threat is introduced. The material point is T-06-12's compliance
dimension — mitigated entirely by the mandatory `[Placeholder]` convention (no fabricated legal data
ships).
</threat_model>

<verification>
- `git grep -n "Moscow\|Moskova\|PAO Sberbank\|Sosenkoye\|Kommunarka\|40802810638000019658\|30101810400000000225" -- messages/` → 0 (Gap 1 closed: no RU legal/bank residue in either locale)
- `git grep -n "american-creator.ru" -- src/components/Footer.tsx` → 0 (Gap 2 closed: no RU-project domain in the copyright)
- `src/components/Footer.tsx` contains `{new Date().getFullYear()} &copy; American Creator` (dynamic year, no domain)
- EN/TR parity: both files parse; identical key sets; 283/283 keys each (value-only swap, no key add/remove)
- Every swapped legal value carries the literal `[Placeholder]` (no fabricated address/VKN/IBAN/bank)
- `npx tsc --noEmit` → 0 errors
- Regression guard (non-blocking): `npx vitest run` shows no NEW failures vs the documented pre-existing baseline (3 `server-api.test.ts` ARM-shape failures); previously-verified TR swaps (phone/email/socials/payment/nav, truths #10-13) unchanged
- Human-check: `/en/contacts` + `/tr/contacts` legal block reads TR `[Placeholder]` disclosure; any-page footer copyright reads "© {current year} American Creator"
</verification>

<success_criteria>
- Gap 1 (blocker) closed: contacts.legalLine1-5 in both locales are the TR `[Placeholder]` seller-identity disclosure; zero RU Moscow/PAO-bank/RUB-account residue; CLEAN-01/CLEAN-02 residue eliminated.
- Gap 2 (warning) closed: footer copyright is a neutral dynamic-year brand line, no RU-project domain (CLEAN-02).
- No fabricated legal data shipped ([Placeholder] convention honored); EN/TR parity held (283/283, no key add/remove); tsc clean; no regression to prior verified TR swaps.
</success_criteria>

<output>
Create `.planning/phases/06-oms-tr/06-06-SUMMARY.md` when done.
</output>
