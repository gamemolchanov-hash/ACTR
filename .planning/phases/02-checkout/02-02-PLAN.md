---
phase: 02-checkout
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - package.json
  - src/components/StripeEmbeddedCheckout.tsx
  - src/app/checkout/page.tsx
  - src/app/checkout/success/page.tsx
  - src/app/basket/page.tsx
  - .env.example
autonomous: false
requirements: [CART-02, CART-03, CART-04, CART-05, CART-06]
user_setup:
  - "make up (autoCRM demo-tenant BFF на :4000) + npm run dev (ACTR)"
  - "В demo storefront должен быть payment_config со Stripe test-ключами (ui_mode=embedded) — иначе /payment/create-session вернёт 400; при необходимости настроить demo storefront в Portal"
  - "Stripe тест-карта 4242 4242 4242 4242, любая будущая дата/CVC — для проверки оплаты"
must_haves:
  truths:
    - "Корзина показывает результат ARM /cart/validate (сток/цена/итог) в валюте заказа"
    - "Доставка: чекаут зовёт /shipping/rates (country=TR по умолчанию + postalCode) и даёт выбрать тариф; method уходит в заказ"
    - "Промокод применяется через ARM /promo/validate (code + subtotal)"
    - "Заказ создаётся через ARM POST /orders, затем POST /payment/create-session → Stripe Embedded Checkout рендерится inline"
    - "После оплаты страница подтверждения тянет заказ через GET /orders/{id} и показывает номер/итог/валюту"
    - "Гость-чекаут: без обязательного логина; loyalty/KDV/юр-текст НЕ добавляем (Phase 3/5)"
  artifacts:
    - path: "src/components/StripeEmbeddedCheckout.tsx"
      provides: "Stripe Embedded Checkout под Next 14 + MUI (адаптация FBG-компонента)"
    - path: "src/app/checkout/page.tsx"
      provides: "Чекаут на ARM: shipping-rates селектор, ARM-промо, создание заказа + Stripe embedded"
    - path: "src/app/checkout/success/page.tsx"
      provides: "Подтверждение заказа из GET /orders/{id}"
  key_links:
    - from: "src/app/checkout/page.tsx"
      to: "createOrder → createPaymentSession → StripeEmbeddedCheckout"
      via: "ARM orders + payment session (embedded)"
      pattern: "clientSecret/publishableKey"

<objective>
Пересобрать UI корзины/чекаута/подтверждения под ARM и встроить Stripe Embedded Checkout (test mode),
заменив СДЭК-доставку, COUNTRIES=RU/BY/KZ, PayKeeper form-post и ₽-формат на TR/ARM/Stripe.
Дизайн AC и вёрстку сохраняем — меняем только источники данных и платёжный блок.

Purpose: довести Phase 2 до рабочего гостевого заказа с оплатой через ARM.
Output: рабочий чекаут end-to-end на demo-тенанте со Stripe-тест-картой.
Reference (паттерн, НЕ копипаст — там React/Vite): FBG `src/components/StripeEmbeddedCheckout.tsx`
и `src/pages/CheckoutPage.tsx` (создание сессии, монтаж embedded).
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/02-checkout/02-01-PLAN.md
@src/app/checkout/page.tsx
@src/app/checkout/success/page.tsx
@src/app/basket/page.tsx
@src/providers/CartProvider.tsx
@src/lib/api.ts
# FBG reference: /home/lexun/work/puz/FBG/src/components/StripeEmbeddedCheckout.tsx
#               /home/lexun/work/puz/FBG/src/pages/CheckoutPage.tsx (Stripe wiring ~line 271)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stripe deps + StripeEmbeddedCheckout (Next 14 + MUI)</name>
  <files>package.json, src/components/StripeEmbeddedCheckout.tsx</files>
  <read_first>/home/lexun/work/puz/FBG/src/components/StripeEmbeddedCheckout.tsx, package.json</read_first>
  <action>
  `npm i @stripe/stripe-js @stripe/react-stripe-js`.
  Создать `src/components/StripeEmbeddedCheckout.tsx` ('use client') — порт FBG-компонента:
  props `{ clientSecret: string; publishableKey: string }`; `loadStripe(publishableKey)` через
  `useMemo([publishableKey])` (одна промис-инстанция на ключ); рендер
  `<EmbeddedCheckoutProvider stripe={stripePromise} options={{clientSecret}}><EmbeddedCheckout/></EmbeddedCheckoutProvider>`.
  Обернуть в MUI `<Box>` вместо tailwind-класса. Грузить компонент в чекауте через `next/dynamic`
  с `ssr:false` (Stripe — только клиент).
  </action>
  <verify>tsc проходит; компонент импортируется без SSR-ошибок</verify>
  <acceptance_criteria>
    - package.json содержит @stripe/stripe-js и @stripe/react-stripe-js
    - StripeEmbeddedCheckout.tsx экспортирует дефолт с props {clientSecret, publishableKey}
  </acceptance_criteria>
  <done>Stripe embedded компонент готов</done>
</task>

<task type="auto">
  <name>Task 2: checkout/page.tsx — доставка (shipping/rates) + страна TR + ARM-промо</name>
  <files>src/app/checkout/page.tsx</files>
  <read_first>src/app/checkout/page.tsx, src/lib/api.ts</read_first>
  <action>
  Удалить СДЭК-блок: `CDEK_OPTIONS`, `searchCdekCities`/`fetchCdekPoints`-эффекты, city-code/points
  state, pickup-point UI. Заменить на доставку через ARM:
  - Поля адреса: street/building/apartment/city/zip + страна. `COUNTRIES` → TR-ориентир;
    хранить и слать **ISO-2** код страны (по умолчанию `TR`). (Список можно из ARM `/countries`,
    если есть — иначе минимальный {TR}.)
  - При наличии country+postalCode+items вызывать `fetchShippingRates({country, postalCode, items})`,
    показать список тарифов (RadioGroup: name + carrier + цена + срок). Выбранный `rate.id` → `shipping.method`,
    `rate.price` → `shipping.cost`. Обработать `fedex_configured:false`/пустой rates —
    показать сообщение «доставка временно недоступна», не блокировать жёстко в dev.
  - Промокод: `validatePromo(code, subtotal)` (новая сигнатура). Скидку из `discount_amount`.
  - Итог: subtotal (из validateCart) − promoDiscount + shipping.cost. Формат валюты — по валюте
    заказа/storefront (НЕ хардкод ₽; см. Task 3 — общий `fmtMoney(currency)`).
  - Сборка заказа: `createOrder({ customer, shipping:{...,country,cost,method}, items, comment?, promoCode? })`.
  Гость-чекаут: авторизация опциональна (если залогинен — предзаполнить адрес из getMyAddresses можно
  оставить, но не требовать). loyalty НЕ добавлять.
  </action>
  <verify>tsc; в браузере чекаут показывает тарифы доставки и применяет промокод</verify>
  <acceptance_criteria>
    - grep CDEK|cdek|searchCdekCities|POSTAMAT|'Россия' в checkout/page.tsx → пусто
    - checkout вызывает fetchShippingRates и createOrder (ARM)
    - страна уходит как ISO-2, по умолчанию TR
  </acceptance_criteria>
  <done>Чекаут собирает ARM-заказ с доставкой TR и промокодом</done>
</task>

<task type="auto">
  <name>Task 3: checkout — оплата Stripe embedded + подтверждение из /orders/{id}</name>
  <files>src/app/checkout/page.tsx, src/app/checkout/success/page.tsx</files>
  <read_first>src/app/checkout/page.tsx, src/app/checkout/success/page.tsx</read_first>
  <action>
  Платёжный поток (заменить PayKeeper form-post):
  1. Сабмит → `createOrder(...)` → получить `{id, number, total, currency}`.
  2. `createPaymentSession(order.id, successUrl, cancelUrl)` где
     `successUrl = ${origin}/checkout/success?order=${order.id}` (Stripe вернёт сюда),
     `cancelUrl = ${origin}/checkout`.
  3. Если ответ embedded (`clientSecret`+`publishableKey`) — смонтировать `<StripeEmbeddedCheckout>`
     (через next/dynamic ssr:false) inline вместо формы заказа. Если ответ hosted (`redirectUrl`) —
     `window.location.assign(redirectUrl)` (фолбэк, на случай ui_mode=hosted у storefront).
  4. На submit очистить корзину НЕ раньше успешного перехода к оплате (сток-резерв ARM делает при создании заказа).
  `success/page.tsx`: читать `order` (UUID) из query, `fetchOrder(id)` → показать `number`, `total`,
  `currency`, статус. Заменить хардкод `fmt = ...' ₽'` на `Intl.NumberFormat` по `order.currency`.
  Очистить корзину (`clearCart`) здесь — точка реального завершения. (Аналитику purchase НЕ добавляем
  в Phase 2 — деплой-трек.)
  Вынести общий `fmtMoney(amount, currency)` (напр. в src/lib/money.ts) и использовать в checkout/basket/success.
  </action>
  <verify>e2e: тест-картой 4242 оплата проходит, success тянет заказ из ARM</verify>
  <acceptance_criteria>
    - checkout: после createOrder вызывается createPaymentSession и рендерится StripeEmbeddedCheckout
    - grep paymentUrl|paymentFields|PayKeeper в checkout → пусто
    - success/page.tsx вызывает fetchOrder и форматирует по order.currency (нет хардкода ' ₽')
  </acceptance_criteria>
  <done>Оплата Stripe (test) и подтверждение заказа работают</done>
</task>

<task type="auto">
  <name>Task 4: basket/page.tsx — итоги ARM + валюта + передача промо в чекаут</name>
  <files>src/app/basket/page.tsx</files>
  <read_first>src/app/basket/page.tsx, src/lib/api.ts</read_first>
  <action>
  Убедиться, что корзина использует `validateCart` (ARM) для строк/итога и форматирует через
  `fmtMoney(currency)` (не ₽). Промокод-инпут (если есть) — на `validatePromo(code, subtotal)`;
  передача выбранного промо в чекаут через sessionStorage (`checkout_promo`) сохраняется.
  Убрать BOGO-плашки/`auto_promo` (у ARM нет). Кнопка «Оформить заказ» → /checkout.
  </action>
  <verify>tsc; корзина показывает ARM-итоги в валюте storefront</verify>
  <acceptance_criteria>
    - basket использует validateCart (ARM) и fmtMoney
    - grep '₽'|auto_promo|BOGO в basket/page.tsx → пусто (или обоснованный остаток)
  </acceptance_criteria>
  <done>Корзина на ARM-итогах</done>
</task>

<task type="auto">
  <name>Task 5: e2e верификация гостевого чекаута</name>
  <files></files>
  <action>
  С `make up` (demo) + `npm run dev`. Через Claude in Chrome пройти: каталог → добавить товар →
  /basket (ARM-итог) → /checkout: заполнить адрес (country=TR, postalCode), выбрать тариф доставки,
  применить промокод (если в demo есть), создать заказ, оплатить тест-картой 4242 в Stripe Embedded,
  вернуться на /checkout/success → заказ виден (номер/итог/валюта). Проверить console чистый,
  /api 2xx, ARM_STOREFRONT_KEY не в .next/static. Python-проба `GET /orders/{id}` подтверждает заказ в ARM.
  </action>
  <verify>полный гостевой заказ оформлен и оплачен (test mode), подтверждение из ARM</verify>
  <acceptance_criteria>
    - Гостевой заказ создан в ARM и оплачен Stripe test card
    - /checkout/success показывает заказ из GET /orders/{id}
    - console без ошибок; ключ не в бандле
  </acceptance_criteria>
  <done>Phase 2 подтверждён end-to-end</done>
</task>

</tasks>

<verification>
- [ ] npm run build / tsc --noEmit проходит
- [ ] Корзина/чекаут/подтверждение — все суммы в валюте storefront (нет ₽-хардкода)
- [ ] Доставка через /shipping/rates, промо через /promo/validate, заказ через POST /orders
- [ ] Оплата: Stripe Embedded Checkout inline, тест-карта 4242 проходит
- [ ] success тянет заказ из GET /orders/{id}
- [ ] СДЭК/PayKeeper/RU-страны удалены; ключ server-side
</verification>

<success_criteria>
- Гость оформляет и оплачивает заказ через ARM (Stripe test mode) на дизайне AC
- Доставка под TR рассчитывается через /shipping/rates
- Промокод применяется через ARM
- Страница подтверждения показывает заказ из ARM
- KDV/юр-UI/loyalty НЕ затронуты (отложены в Phase 5/3)
</success_criteria>

<output>
After completion, create `.planning/phases/02-checkout/02-02-SUMMARY.md`
</output>
