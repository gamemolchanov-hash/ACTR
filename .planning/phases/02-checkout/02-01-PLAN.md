---
phase: 02-checkout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/arm-types.ts
  - src/lib/arm-adapter.ts
  - src/lib/api.ts
autonomous: true
requirements: [CART-01, CART-02, CART-03, CART-04, CART-06]
user_setup: []
must_haves:
  truths:
    - "validateCart шлёт ARM-форму {items:[{distributorProductId,quantity}]} на POST /cart/validate с X-Currency"
    - "validatePromo шлёт ARM-форму {code, subtotal} на POST /promo/validate (без items/phone)"
    - "fetchShippingRates вызывает GET /shipping/rates?country&postalCode&items&currency и отдаёт rates[]"
    - "createOrder шлёт ARM CreateOrder {customer, shipping{...,country ISO-2, cost, method}, items[{distributorProductId,quantity}], comment?, promoCode?}"
    - "createPaymentSession вызывает POST /payment/create-session и отдаёт {clientSecret, publishableKey, redirectUrl?}"
    - "fetchOrder(id) вызывает GET /orders/{id} и отдаёт ARM Order"
    - "OMS/CDEK-функции (searchCdekCities, fetchCdekPoints, fetchDeliveryServices) и payment_method удалены"
  artifacts:
    - path: "src/lib/api.ts"
      provides: "Checkout data-layer на ARM: cart/promo/shipping/order/payment/order-detail"
    - path: "src/lib/arm-types.ts"
      provides: "Типы ARM checkout-ответов (CartValidation, PromoValidation, ShippingRate, OrderCreate, PaymentSession, Order)"
    - path: "src/lib/arm-adapter.ts"
      provides: "Адаптеры ARM→существующие типы витрины (ValidatedCartItem, PromoValidationResult)"
  key_links:
    - from: "src/lib/api.ts"
      to: "/api/storefront/cart/validate · /promo/validate · /shipping/rates · /orders · /payment/create-session"
      via: "axios → route-handler → ARM"
      pattern: "public/arm/storefront"

<objective>
Перепроводить checkout-слой данных витрины (`src/lib/api.ts`) с OMS-контракта (СДЭК, PayKeeper,
`/validate-promo`, `payment_method`) на ARM storefront API. UI пока НЕ трогаем — только функции,
типы и адаптеры. Это фундамент: план 02-02 (UI + Stripe embedded) садится поверх.

Purpose: без ARM-форм запросов корзина/доставка/заказ/оплата не заработают на ARM.
Output: ARM checkout-функции + типы + адаптеры; удалённый OMS/CDEK-слой.
Контекст решений (discuss 2026-06-29): Stripe **embedded**, доставка — `/shipping/rates` как есть,
скоуп — **гость-чекаут**, KDV/юр-UI отложены в Phase 5, loyaltyPointsToRedeem не используется (Phase 3).
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/api.ts
@src/lib/arm-types.ts
@src/lib/arm-adapter.ts
# ARM контракт (источник истины): autoCRM/packs/arm/bff/docs/openapi.yaml
#   GET  /shipping/rates?country&postalCode&currency&items=[{distributorProductId,quantity}]
#        → {fedex_configured, rates:[{id,slug,name,carrier,estimated_days_min,estimated_days_max,price,original_price,is_free,free_threshold,live_rate}]}
#   POST /cart/validate {items:[{distributorProductId,quantity}]} (X-Currency) → {data: CartValidation}
#   POST /promo/validate {code, subtotal} (X-Currency) → {data: PromoValidation}
#   POST /orders CreateOrder → {data:{id,number,total,currency}}   (поле промокода: body.promoCode — см. storefront-api.ts:1262)
#   GET  /orders/{id} (гость по UUID) → {data: Order}
#   POST /payment/create-session {orderId,successUrl,cancelUrl} → {data:{sessionId,clientSecret?,publishableKey?,redirectUrl?}}
</context>

<tasks>

<task type="auto">
  <name>Task 1: ARM checkout-типы + адаптеры</name>
  <files>src/lib/arm-types.ts, src/lib/arm-adapter.ts</files>
  <read_first>src/lib/arm-types.ts, src/lib/arm-adapter.ts, src/lib/api.ts</read_first>
  <action>
  `arm-types.ts` — добавить типы ARM checkout-ответов (по openapi):
  - `ArmCartValidation { items: {distributorProductId, valid, name?, sku?, unitPrice?, quantity, available?, lineTotal?, image?, error?}[]; subtotal: number; allValid: boolean }`
    (сверить точные имена полей CartValidation в openapi components.schemas — взять как есть).
  - `ArmPromoValidation { valid: boolean; code?: string; discount_type?: string; discount_value?: number; discount_amount?: number; free_shipping?: boolean; description?: string | null; error?: string }`.
  - `ArmShippingRate { id; slug: 'economy'|'standard'|'express'|'overnight'; name; carrier; estimated_days_min; estimated_days_max; price; original_price?; is_free?; free_threshold?: number|null; live_rate?: boolean }`.
  - `ArmShippingRatesResponse { fedex_configured: boolean; rates: ArmShippingRate[] }`.
  - `ArmOrderCreateResponse { data: { id: string; number: string; total: number; currency: string } }`.
  - `ArmPaymentSession { sessionId: string; clientSecret?: string; publishableKey?: string; redirectUrl?: string }`.
  - `ArmOrder { id; number; date_created; total; currency; status:{code,name,color}; track_number?; track_url?; items:{id,quantity,unit_price,total,product:{name,sku}}[] }`.
  `arm-adapter.ts` — добавить адаптеры в СУЩЕСТВУЮЩИЕ типы витрины (чтобы UI-план менял минимум):
  - `armToValidatedCart(v: ArmCartValidation): { items: ValidatedCartItem[]; subtotal: number; allValid: boolean }`
    — `productId = it.distributorProductId` (витрина оперирует этим id), остальные поля 1:1.
  - `armToPromoResult(p: ArmPromoValidation): PromoValidationResult` — `valid`, `code`, `discount_type`
    (привести к 'percent'|'fixed' где применимо), `discount_value`, `discount_amount`, `description`, `error`.
  </action>
  <verify>tsc --noEmit без ошибок; типы экспортируются</verify>
  <acceptance_criteria>
    - arm-types.ts содержит ArmCartValidation, ArmShippingRate, ArmOrder, ArmPaymentSession
    - arm-adapter.ts экспортирует armToValidatedCart, armToPromoResult
    - tsc проходит
  </acceptance_criteria>
  <done>Типы и адаптеры ARM checkout готовы</done>
</task>

<task type="auto">
  <name>Task 2: api.ts — перепроводка checkout-функций на ARM + X-Currency</name>
  <files>src/lib/api.ts</files>
  <read_first>src/lib/api.ts, src/lib/arm-adapter.ts, src/app/checkout/page.tsx</read_first>
  <action>
  ВАЖНО: `CartItem.productId` уже хранит `distributorProductId` (Phase 1: `Product.id = dp.id`).
  Поэтому в ARM-запросы мапим `{ distributorProductId: i.productId, quantity: i.quantity }` —
  тип `CartItem` и CartProvider НЕ трогаем (CART-01 уже выполнен на уровне данных).

  X-Currency: добавить хелпер `currencyHeader()` → `{ 'X-Currency': process.env.NEXT_PUBLIC_STOREFRONT_CURRENCY || 'USD' }`
  (dev demo-дистрибьютор; прод → TRY через env, Phase 7). Слать его на cart/promo/shipping/order.

  Переписать/добавить функции:
  - `validateCart(items)` → `POST /cart/validate` body `{ items: items.map(toArm) }`, headers currencyHeader().
    Вернуть `armToValidatedCart(data.data)` в существующей форме `{ data: { items, subtotal, allValid } }`.
    Убрать BOGO `auto_promo` (у ARM нет) — поле выпилить из возвращаемого типа.
  - `validatePromo(code, subtotal)` — НОВАЯ сигнатура (было `(code, items, phone)`):
    `POST /promo/validate` body `{ code, subtotal }`, currencyHeader(). Вернуть `{ data: armToPromoResult(data.data) }`.
  - `fetchShippingRates({ country, postalCode, items, currency? })` — НОВАЯ:
    `GET /shipping/rates` params `{ country, postalCode, currency, items: JSON.stringify(items.map(toArm)) }`,
    currencyHeader(). Вернуть `ArmShippingRatesResponse`.
  - `createOrder(payload)` — ARM CreateOrder shape:
    `{ customer:{name,phone,email?}, shipping:{address?,street?,building?,block?,apartment?,city?,zip?,country (ISO-2),cost?,method?}, items: items.map(toArm), comment?, promoCode? }`,
    currencyHeader(). Тип `CreateOrderPayload` переписать под это (убрать delivery_service/CDEK/payment_method).
    Вернуть `ArmOrderCreateResponse`.
  - `createPaymentSession(orderId, successUrl, cancelUrl)` — НОВАЯ:
    `POST /payment/create-session` body `{ orderId, successUrl, cancelUrl }`. Вернуть `{ data: ArmPaymentSession }`.
  - `fetchOrder(id)` — НОВАЯ: `GET /orders/{id}`. Вернуть `{ data: ArmOrder }` (гость по UUID; токен пока не нужен).
  УДАЛИТЬ OMS-only: `searchCdekCities`, `fetchCdekPoints`, `fetchDeliveryServices`, типы `CdekCity`,
  `CdekPoint`, `DeliveryService`, старый `validatePromo`-контракт, `paymentUrl/paymentFields` из ответа заказа.
  (Их call-sites в UI чинит план 02-02 — здесь только data-layer; tsc на UI временно может ругаться,
   это ОК для wave 1, фиксируется в 02-02. Если хочешь зелёный tsc сразу — оставить заглушки-реэкспорты
   нельзя; вместо этого 02-02 идёт сразу следом в той же фазе.)
  </action>
  <verify>python-пробы через dev-прокси (нужен `make up` demo + `npm run dev`):
    POST /api/storefront/cart/validate {items:[{distributorProductId:<demo dp id>,quantity:1}]} → 200;
    POST /api/storefront/promo/validate {code:'TEST',subtotal:100} → 200/валидный ответ;
    GET  /api/storefront/shipping/rates?country=TR&postalCode=34000&items=[...] → 200 (rates или fedex_configured:false);
    POST /api/storefront/orders {ARM CreateOrder} → 201 {id,number,total,currency};
    POST /api/storefront/payment/create-session {orderId} → 200 {clientSecret|redirectUrl}.
    Тесты — python в tests/ (requests), не curl.</verify>
  <acceptance_criteria>
    - api.ts: createOrder шлёт items как {distributorProductId,quantity} и promoCode
    - api.ts содержит fetchShippingRates, createPaymentSession, fetchOrder
    - grep CDEK|searchCdekCities|fetchDeliveryServices|payment_method в api.ts → пусто
    - X-Currency уходит на cart/promo/shipping/order
  </acceptance_criteria>
  <done>Checkout data-layer полностью на ARM</done>
</task>

</tasks>

<verification>
- [ ] tsc по api.ts/arm-types/arm-adapter чистый (UI-call-sites чинит 02-02)
- [ ] python-пробы 5 ARM checkout-эндпоинтов через dev-прокси возвращают ожидаемое
- [ ] OMS/CDEK-слой удалён из api.ts
- [ ] X-Currency инъектится на checkout-запросы
</verification>

<success_criteria>
- validateCart/validatePromo/fetchShippingRates/createOrder/createPaymentSession/fetchOrder работают против ARM demo
- Тип заказа и items соответствуют ARM CreateOrder (distributorProductId, promoCode, country ISO-2)
- Никаких СДЭК/PayKeeper/payment_method в data-layer
</success_criteria>

<output>
After completion, create `.planning/phases/02-checkout/02-01-SUMMARY.md`
</output>
